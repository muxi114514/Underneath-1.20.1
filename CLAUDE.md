# Underneath 项目记忆（Claude Code 自动加载）

Forge **1.20.1** 模组，`mod_id=underneath`，group=`com.mx.underneath`，作者 MX。
定位：把 **RLCraft Dregora** 的 **1.12.2 OTG 预设维度「Underneath」** 重建到 1.20.1。
**仅服务 1.20.1 RLC 整合包**，可依赖包内其他 mod，不追求独立性。
原预设作者 NLBlackEagle & Shivaxi。

---

## 一、Underneath 是什么（关键前提）

- 原 `Underneath-2.0.jar`（`D:\relink\114514\.minecraft\versions\RLCD1.1.2.b\mods\`）不是普通维度 mod，
  是用 **WorldPacker/PresetPacker**（`E:\WorldPacker-1.16.4`）打包的 **OTG 预设**，mcversion **1.12.2**，
  只含 1 个胶水类、运行时硬依赖 OTG。内容全是 OTG 预设数据：WorldConfig.ini（`BiomeMode:FromImage`、
  无天光/永夜/暗红雾/SpawnY260、所有原版结构关闭、`IsOTGPlus`）、26 个 `.bc` 群系、
  ~1865 个 BO3/BO4（185 单体树 + 1674 BO4 零件/根）、37936 个 `.nbt`（方块实体+loot）、map.png。
- **1.20.1 无 OTG（Forge）**（最新仅 1.16.5/1.12.2）→ 直译不可能，必须原生重建。

## 二、已定方案

**原生 1.20.1 地形（砍掉卡顿的 OTG 地形引擎）＋ 直接读取 OTG 的 BO 建筑内容。**
- BO 加载分两层：**L1 单体直读**（185 树 + 大量无分支 BO4 零件，简单）；
  **L2 分支拼装器**（`CityGrid` 地下巨城 / `access_duct` / `GodlySpike`，根文件用 `Branch(...)` 递归拼零件，
  移植 OTG 算法，**全工程最大硬骨头**）。
- 与 EnigmaticEchoes 无关，本模组独立（框架照搬 RLStructure/BetterSurvival）。
- 群系分布先不追 map.png 精确分区（想要可后期加自定义 BiomeSource 读图）。

## 三、方块来源与替换（目标 RLC 包已含大部分）

结构主体 = **BetterNether**（47 万处引用，✅包内）；地形/结构还用 Quark✅、BOP✅、DefiledLands✅、
Lycanites(1.20.1-alpha✅)、DynamicTrees✅、ScalingHealth✅。**需替换**：
- `srparasites:*`（门框+污染地形 11 万处）→ **EPCA「归寄万物」`epca:`**
  （`[1.20.1-Forge]RLcraft Reforged\mods\End-Parasitize and Convert All-0.3.5e-1-1.20.1.jar`，
  整套 `infested_*` 方块，门框可用 `epca:infested_infested_stone`/`nethersea_brand_solid`）。
- `biomesoplenty:blood/flesh`（维度的水/冰，BOP1.20.1 已删）→ 自定义流体/红染水（**待拍板**）。
- `eaglemixins:deepslate`/`contenttweaker:deepslate_*_ore`（铁金钻煤青金红石）→ **1.20.1 原版** deepslate/矿。
- `contenttweaker:deepslate_{thorium,lithium,lead,boron,crystal}_ore`、`nuclearcraft:*`、`rustic/charm/家具类` → 原版替代或省略（**待拍板**）。
- BO 方块是 **1.12 名字+meta**（`STONE:5`/`WOOL:1`/`betternether:名:meta`），映射表照 OTG `DefaultMaterial` 反查。

## 四、分期计划

- **P0 · 工程骨架 + 空维度【已完成，待首次编译验证】** ← 见「六」
- **P1 · 方块映射表 + 单体 BO 直读（L1）**：全量方块清单→`BlockMapper`；BO 文本解析器；单体放置 Feature；.nbt 方块实体+loot。
- **P2 · 群系 + 表层(SurfaceRules) + 矿物(Ore feature)**：核心 biome + 原生 noise 地下地形调参。
- **P3 · BO4 分支拼装器（L2·核心硬骨头）**：移植 OTG `CustomStructurePlotter`/`BO4CustomStructure`/`BranchDataItem`；攻坚 CityGrid/access_duct/GodlySpike。
- **P4 · 打磨**：loot/刷怪精修、传送门粒子音效、（可选）map.png BiomeSource、低重力 mixin。
- **P0b · 传送门**（P0 未做，见「六」末）：门框换料 + 点燃 + 传送逻辑。**原版参数已从 WorldConfig.ini 实锤（2026-09-14）**：`PortalColor: darkred`+`PortalParticleType: townaura`；维度出生点 `SpawnPointSet 0/260/0`（穹顶之上）、`TeleportToSpawnOnly` 先 true 后 false（取后者=按坐标映射传送）、`CanRespawnHere: true`/`RespawnDimension: 3`、`DoMobSpawning: true`（biome 表空=实际靠笼）。**同文件实锤 `GravityFactor: 0.07`＝维度低重力是原版核心风味**（1.20 可走 `ForgeMod.ENTITY_GRAVITY` attribute 免 mixin）。
  **方案已拍板（2026-09-14·用户设计）——双侧借现成资产**：①主世界入口=**深暗古城中央巨门**（reinforced_deepslate 框，内空~18×4-5 落在原版 PortalShape 宽 2-21×高 3-21 原生范围内→只换框材料判定；强化深板岩不可获取=天然唯一入口门槛；实装时实地看 city_center 底边是否平整、不平做宽容）；②深渊出口=**portal_ruins_low 废墟点火修复**（几何已实锤：`portal_overworld_low.bo3`=ashenstonepolished 柱础+双 ashenstonepillar 柱高 6+门楣 y=7 残 2 块**缺 2 块**→玩家补 2 块灰烬石闭合成 2×6 内空门框；**门体=Lycanites 灰烬石系（用户纠正，验证属实），srparasites 碎石只是地面瓦砾非门框**；1.20 灰烬石全家直传实锤）；③**门框白名单**：主世界侧=reinforced_deepslate、深渊侧=ashenstone/ashenstonepillar/ashenstonepolished（WorldConfig 的 srparasites 名单=OTG"任意处搭门"判定，被古城方案取代，弃用）；④**单程语义+回程门材料反接（用户拍板）**：进=古城→穹顶顶面，落点找/造**强化深板岩小锚点门**（不可挖+门帘基岩抗爆=怪物/玩家/爆炸都拆不掉的永久锚点）；出=底腔废墟→主世界地表，找/造**灰烬石门**（可拆"战利品门"，拆后玩家自搭点不亮=白名单分侧的自然推论，零特殊标记）；坐标 1:1；128 格内已有锚点复用不重复造。
  **P0b+低重力已实装（2026-09-14·待编译）**：新 `portal/` 包四类+`event/LowGravityHandler`——`UnderneathPortalShape`（原版 PortalShape 算法泛化重写：点燃白名单分侧（灰烬石惰性按名解析零编译依赖）、存续校验用并集、isCompleteAt 供门帘熄灭判定）；`UnderneathPortalBlock`（参照 NetherPortalBlock：AXIS/updateShape 熄灭链/entityInside cooldown 刷新+传送/**MYCELIUM 粒子=1.12 townaura**+PORTAL_AMBIENT）；`UnderneathTeleporter`（ITeleporter：getPortalInfo 找/造门、placeEntity **repositionEntity.apply(false)** 跳过原版下界门逻辑；穹顶门 heightmap MOTION_BLOCKING 取顶面 clamp[240,250] 破洞列悬空 249 造台；主世界门 MOTION_BLOCKING_NO_LEAVES 地表；4×5 门+6×3 平台+清空区）；`PortalAnchorSavedData`（LongList 锚点+失效自清，主线程访问免预热）；`PortalIgniteHandler`（RightClickBlock 打火石→门形成立即放门帘+cancel，不成立放行原版点火）；~~LowGravityHandler~~→**`FallDamageHandler`（2026-09-14 用户拍板改版：重力回原版——0.07 观感太怪已弃；摔距 ×0.1=130 格天降≈5 心重伤不死、30 格内近无伤，调手感改 FACTOR）**。注册：ModBlocks.PORTAL（strength(-1,3.6M) 光11 PushReaction.BLOCK）+blockstates/portal_ns|ew 模型（json render_type translucent 免代码）+`dev/paint_portal.py` 自绘 32 帧暗红旋涡门帘+双语 lang。
  **血焰打火石=传送门唯一点火钥匙（2026-09-14·用户拍板·已实装）**：合成=回响碎片+`betternether:cincinnasite`（无序，配方带 forge:mod_loaded 条件）——古城的材料开古城的门；原版打火石不再能点门（PortalIgniteHandler 判定改 `ModItems.BLOOD_FLINT_AND_STEEL`）。普通使用打出**血焰**（`block/BloodFireBlock`=SoulFireBlock 模式：烧实体不蔓延、光12、只换色本质是火；营火/蜡烛照常 super）。贴图=`dev/paint_flint.py` 调色管线（打火石按亮度分区：暗区燧石→echo_shard 梯度青、亮区钢→cincinnasite 梯度金，色板从两物品贴图现场采样；血焰=原版 fire_0/1 色相压向血红 V×0.88，含 mcmeta 复制）。**废墟门箱子语境改造**：`portal_overworld_low.bo3` 的箱=`bone_with_flint.nbt`→表 `underneath:underneath/bone_with_flint`→三张 flint 子表定点替换（port_loot_tables `TABLE_TWEAKS`，勿全局映射）：flint_1 打火石→血焰打火石/flint_2 燧石+铁粒→回响碎片+辛西纳石/flint_3 燧石→回响碎片；404 表重跑+三维校验三零。
  **P0b 验收（重编后）**：①合成血焰打火石（回响碎片+辛西纳石）；创造栏工具页有之；对地面右键=血红色火焰；②主世界 `/locate structure minecraft:ancient_city`→中央巨门内用**血焰打火石**点燃→整幅暗红门帘（原版打火石应点不成门；底边不平则观察 shape 失败情况回报）；③进门→穹顶顶面出现小强化深板岩锚点门、身处其中；试挖/爆=不可破坏；④重力=原版、从穹顶跳下主腔摔伤≈5 心重伤不死（摔距 ×0.1）；⑤底腔找 portal_overworld 废墟→开 Remnant Flint Chest 应出血焰打火石/回响碎片/辛西纳石→补 2 块灰烬石点燃 2×6 门→出去=主世界地表灰烬石门；⑥拆主世界灰烬门→门灭；原地重搭点火=只出血焰不成门 ✓ 语义；⑦回穹顶锚点门可反复往返；128 格内不重复造门。

## 五、构建

- JDK17：`C:\Program Files\Zulu\zulu-17`；`org.gradle.daemon=false`（gradle.properties 已设）。
- `gradlew build` → 产物 `build/libs/Underneath-1.20.1-1.0.jar`。
- **不主动编译**：改完代码由用户自行构建。

## 六、从这里继续（会话续接·下次先读这段）

> **⚡ 当前状态快照（2026-09-14·P3 五测拼缝大修**用户实测通过=P0~P3 主线收官**·下一步 P0b 传送门/P4 打磨待拍板）**：P0/P1/P2 完成；城市=OTG 1:1 移植（`OTGStructureExpander`+`CityPlotter`+废墟散布系，「P3·OTG 忠实移植」条目），五轮实测修复线：**首测三修**（access_duct 组间距/旋转传参/全高度 carver+carver JSON 两崩）→城市生成；**二测三修**（populate 2×2 域=destructor 降 28 倍/sea_level 63 血池/Entity 行）→城市基本正常；**三测六修**（ReplacedBlocks 抽干底腔+岩浆→玄武岩/邻接形状修正=门板栏楼梯钟乳石/映射补两族=品红真相/chestfiller 箱 rndNbt/裸笼→寄生虫/武器海回归 270→404 表）；**四测收尾**（捆包 49 非法物品清零=富表复活的最后一环/传送门 floor 下界 18→12/补修：defiledspartan+spartanshields 真身归位+唱片池 34 碟）；**五测拼缝大修**（城碎/路断头错位真凶=光锥 region 拼接对贪心密铺不收敛，改 SavedData 增量贪心=OTG structurecache 同构，见「P3·五测」条目）。
> **🔴 新对话第一件事**：P0b 传送门+血焰打火石+低重力已实装待编译（验收清单见「P0b」条目）。**遗迹 .bc 复核+worldgen 2D 缓存已完成（2026-09-14）**：①水晶板漏网已接线——`Tree(50,CrystalPlate-1..16 各100)` 只在 Ancient Remnants 主+R-ONE（CrystalPlate=hadron_collider 对撞机部件，randomY 84 固定，4 token 映射全在）；remnants_edge/wall/outer_edge 多挂的 portal_ruins_low 已移除（原版无此行，Edge/Wall 只有 Sapling）；gen_biomes 重跑散布 23 对。②`OTGTerrainMath` 2D noiseHeight ThreadLocal 单列缓存——离线实证等价（乱序 2 万点零差异）+列序提速 42%（154↔267ms/chunk）。**四项批示已落地（2026-09-14）**：A=重力回原版+摔距 ×0.1（`event/FallDamageHandler`，LowGravityHandler 已删）；B=**3D 角点三线性插值**（`OTGTerrainMath.CORNER_INTERPOLATION=true` 一行开关可回退直采；离线对拍剖面五段结构保持/方块一致率 98.95%/边界差 ≤3 格=原版棱面感；**15ms/chunk ≈ 10 倍提速**，角点 ThreadLocal HashMap 缓存 56 位无碰撞 key）；C1+C2=**config 覆盖层**——IdMapper 三表（entity/item/loot_map）读 `config/underneath/<表名>.json` 条目优先于数据包（首缺自动导出模板=全部近似替代条目，城市笼换怪=改 entity_map.json 的 epca:ripper 行），loot 表整表覆盖=`config/underneath/loot_tables/<同路径>.json`（`event/LootOverrideHandler`，LootTableLoadEvent 期替换，仅 underneath: 域，解析失败保留原表）；C3 VC 数值不调/C4 树苗表不做（用户拍板）。血焰首测"黑背景"已修：**自定义火/门帘等透明类方块的渲染层必须在模型 json 声明 `render_type`（cutout/translucent）**——原版是 ItemBlockRenderTypes 代码表注册、mod 方块默认 solid=透明区渲染成黑（通用坑）；血焰调色顺带保留白热芯层次（v 亮部少压）。**P0b 首测二修（2026-09-14·用户实测）**：①**基岩层回程门**——`Level.getHeight` 对未加载 chunk 返回 minBuildHeight(-64)（进深渊侧恰被 clamp[240,250] 兜住才没炸）→ 修=buildRoof/SurfacePortal 开头 `level.getChunk(x>>4,z>>4)` 强制同步生成再取高度（**通用坑：未加载 chunk 的 heightmap 查询全是 -64**）；②**EPCA 刷怪笼不出怪**——javap 实锤 `checkXxxSpawnRules`：phase gate 只拦 NATURAL/CHUNK_GENERATION（笼子不受限），但**光照<8 检查对 SPAWNER 也生效**→楼体自带灯=笼死；修=SpawnData 一律带 `custom_spawn_rules`(block/sky 0-15 全光照带)——**BaseSpawner 有 custom_spawn_rules 时跳过 SpawnPlacements 谓词**（TileEntityConverter.wrapSpawnData 两路径+BOPlacer.fixBareSpawner 改 NBT 版）；映射 id 全量校验过=73 实体全有效非拼写问题。旧世界已放置的笼子不回溯，新区块生效。**追加实锤（用户补充"黑暗处也不生成"后 javap 对比）**：EPCA 把 flying_carrier/infested_villager/small_incomplete_form 等一批实体的谓词写成「光照<0」＝**恒 false**（正常怪「光照<8」；ifge vs bipush 8+if_icmpge 字节码铁证）——custom_spawn_rules 修复恰好覆盖（跳过整个谓词）；另加 `event/SpawnerRuleHandler`（MobSpawnEvent.SpawnPlacementCheck：SPAWNER+epca ns 一律 ALLOW，不限维度零平衡风险）兜底**手动放笼/刷怪蛋测试**路径。全 jar 无 m_5545_ 实例覆写=检查链只剩光照带+障碍。**城市刷怪笼候选池 config 化（用户拍板 30 种）**：`bo/SpawnerPool`（`config/underneath/spawner_pool.json` id→权重，首缺导出默认、逐条注册校验、重启生效）——名单=EPCA 刷怪蛋全集 排除 头颅 walking_*_head×15/一二阶召唤柱×2/**fins 鳍（用户："陆地出鱼太抽象"）**；黏液/血肉蛋无同名实体→取 `infested_slime_size1`/`living_flesh_size2` 代表，恰 30 种；接线=TileEntityConverter.spawner()（映射后含 epca 实体的笼→整池替换，删 SpawnData 由 Potentials 掷）+fixBareSpawner（裸笼→整池）。**深渊自然刷怪（用户拍板）**：`event/UnderneathNaturalSpawner`（LevelTickEvent·血湖=epca:fins（BloodFluid.isSame 判定源+流动）、陆上=池怪 30 种加权、**无视亮度**——直接 addFreshEntity 绕过 EPCA 谓词与原版位置检查（血=自定义流体原版水生判定不认，biome spawner 路线不通）；防卡=每玩家 64 格 cap（怪 24/鱼 6）+间隔（40/200t）+每次 1 只+getChunkNow 守卫单列扫描，参数在 spawner_pool.json `__natural__` 键可调；biome JSON 空刷怪表保持不动）。**三项批示（2026-09-14·用户）**：①骨堆→骨块（`contenttweaker:bone_pile`→`minecraft:bone_block`；本模组 BONE_PILE 方块注册保留仅建筑不放）；②笼箱翻倍（chestfiller attempts 85/85/85/25→**170/170/170/50**，gen_biomes 重跑）；③**VC 换真身 jar**——用户提供 `variedcommodities-2026-04-06-1.20.1.20251013.jar`（384 物品·modid 不变·**1.12 原名全保留=零映射直传**，该 jar 须入包）：41 种对撞零缺失；pack_items 名单并入 384；port_loot_tables 删全部 VC 映射条目重跑 404 表三零；item_map 删正名条目（直传）、坏前缀 ariedcommodities 3 条改指真身；**删本模组自建**＝ModItems VC 段+UnderneathTiers+UnderneathArmorMaterials+贴图模型 91 文件+lang 41×2 键+mods.toml CC BY-NC 署名段撤除（不再分发素材，Dregora 致谢保留）；残留引用 grep 清零。
**血挂 water tag（用户拍板"本身就是装饰没啥大问题"）**：`data/minecraft/tags/fluids/water.json` 收 blood+flowing_blood——鱼在血中正常游/玩家血中游泳+溺水（合理）；**凝肉交互不受影响**（FluidInteractionRegistry 按 FluidType 与 tag 正交；岩浆碰血也仍凝肉不成石）；无限水判定按流体实例（WaterFluid.isSame）血不参与 ✓；顺带补了**反方向交互**（血流碰水→血这格凝肉，原来只有他液碰血单向）。剩余=编译验收全家（五测+P0b+本轮）。
> **爆裂魔法彩蛋 trigger（2026-09-14·非 OP 可用·两段式咒语）**：玩家 Tab 发现 `explosion` 目标 → 裸 `/trigger explosion`=深渊低语谜语（"血海之面的高度=最后的音节"，游戏内 F3 实测 Y63 可解）→ **`/trigger explosion set 63`**=爆炸音效+粒子+全家桶 24 件；错误数字=嘲讽清零。实现=mod 自带数据包函数（`data/underneath/functions/` init/tick/explosion_success|hint|wrong + `minecraft/tags/functions/load|tick.json`；tick 每刻 re-enable+按分数分档结算，63 先清零故后行不重复匹配）。
> **⚠️ 铁律：绝不主动 `gradlew build/compileJava`**（用户明令；我违反过被制止）。让用户自己构建、把编译错误全文发来我改。
> **🧰 方法论（本项目已验证的三板斧）**：①改 worldgen/城市算法必先 Python 同构模拟（`dev/sim_otg_city.py`=JRandom+展开引擎逐行同构、`dev/sim_plotter.py|sim_plotter5.py`=plotter 全流程——首测/二测的根因都是模拟先复现再修）；②改 loot 表跑 `dev/verify_bundles.py` 三维校验（SNBT/物品名单/属性）三零才算过——**表里一个未知物品 id=解析期废整张表**；③写任何 worldgen JSON 先 `unzip -p 版本jar data/minecraft/worldgen/<同类>/xxx.json` 抄官方原文对拍（FloatProvider 的 uniform=包 `value`+`max_exclusive`，与 HeightProvider 平铺 `max_inclusive` 不同——carver 两崩教训）。
> 历史坑速查：每轮改完**必须**核对 build/libs jar 时间戳更新+覆盖进包再测（反复踩"跑旧 jar"）；测试勿用 `all_loot_test`/`loottest`（原包断链）；城市刷怪笼出寄生虫/马桶箱出可可豆垃圾=1.12 原设定正确；楼体自带 0 刷怪笼、笼箱全靠 chestfiller 楼板散布件=原版设计。

**P0 已实装（待首次编译验证）**，共 4 个 Java + 3 个 JSON + 全套 gradle 框架（照搬 RLStructure，仅改 id/group/描述）：
- 框架：`build.gradle`/`settings.gradle`/`gradle.properties`/`.gitignore`/gradle wrapper/`mods.toml`/`pack.mcmeta`/`underneath.mixins.json`(空插槽)。P0 依赖仅 forge（方块 mod 依赖留到 P1）。
- 主类 `Underneath.java`（入口，P0 无 DeferredRegister）；`init/ModDimensions.java`（维度/维度类型 ResourceKey 常量）。
- 客户端 `client/UnderneathDimensionEffects.java`（无天空 SkyType.NONE + 暗红浓雾 0.4/0.08/0.08）+ `client/ClientSetup.java`（`RegisterDimensionSpecialEffectsEvent` 在 mod 总线注册特效，id=`underneath:underneath`）。
- 数据：`data/underneath/dimension_type/underneath.json`（无天光/永夜 fixed_time=18000/ambient_light=0/暗红 effects/min_y=-64 height=384 对齐 overworld 噪声）+ `data/underneath/dimension/underneath.json`（noise 生成器 settings=`minecraft:overworld`、fixed biome `minecraft:dripstone_caves`——P0 占位地形，P2 换成调好的地下型）。

**P0 验收步骤**（用户重编后）：
1. `gradlew build` 通过、mods 菜单能看到 Underneath。
2. 进游戏 `/execute in underneath:underneath run tp @s 0 100 0`（或先 `/forge dimensions` 确认维度存在）能传送进去。
3. 确认：**无天光**（除方块光外漆黑）、**永夜**（太阳不动/无日月）、**暗红浓雾**。
4. 能站在原生地形上（占位为 overworld 风格石地形）。

**P1 进行中（全量方块普查已完成，关键修正如下——写解析器前必读）**：
- **BO 有长/短两套函数名**（OTG `BO3Loader/BO4Loader` 注册表）：`Block/B、RandomBlock/RB、Branch/BR、WeightedBranch/WBR、Entity/E、Particle/P、Spawner/S、ModData/MD`；BO3 另有 `MinecraftObject/MCO、BlockCheck/BC、BlockCheckNot/BCN、LightCheck/LC、ModCheck/MC、ModCheckNot/MCN`。**BO4 实际数据几乎全用短名 `B(`/`RB(`/`BR(`**，只认长名会漏 99%。
- **分支普查修正：1519/2416 个 BO 带分支**（旧结论"只有个别根"是被短名骗了）。但多数是
  `single_skyscraper_*` 这类**刚性分支**（楼按 16×16 切片、100% 概率固定偏移拼回自己）→
  **架构改为三层**：L1=无分支单体直读；**L1.5=刚性分支加载期扁平化**（递归展开 100% 概率分支成一个大方块列表，覆盖各摩天楼/尖刺）；L2=随机分支 plotting（仅 `CityGrid*`/`access_duct` 级选楼逻辑）。
- **映射表规模**：1022 个 distinct token = **879 个 1.12 原版旧名**（`STONE:5` 式，脚本按 flattening 表机械转换）+ **143 个 mod 方块**（手工填）。工具：`<scratchpad>/block_inventory.py` → 输出 `dev/block_inventory.tsv`（git 忽略）。
- **重头映射已探明**：`quark:iron_plate(:0/:1)` **185 万处=城市主材**（1.20.1 Quark 有 iron_plate/rusty_iron_plate，meta 0/1 对应）；`betternether:quartz_stained_glass:15` 47.6 万处**是 betternether 唯一方块**（一行映射→黑色玻璃类，无需 BetterNether 依赖）；`notreepunching:cobblestone/andesite` 10.7 万（Reforged 有 NTP）；`srparasites` 11.5 万/11 方块→EPCA；`lycanitesmobs:ashenstone*` 6.3 万（1.20.1-alpha 有 ashenstone/ashencrystal/slab/stairs ✓）。
- **两包在位差异**：Reforged 有 EPCA/NTP/DefiledLands(+addons)/Lycanites/BetterNether/BOP/ScalingHealth、**缺 Quark**；supersecret 有 Quark(+Oddities)/DynamicTrees/Lycanites/DefiledLands、**缺 EPCA/NTP/BOP**。
- **三项拍板已定（2026-09-11）**：①**目标包=`D:\relink\114514\.minecraft\versions\supersecret1.20.1updateRL-1.70`**（254 jar：Quark✓+Oddities✓/EPCA 0.147f✓/DynamicTrees✓/DefiledLands 1.10✓/BetterNether 20.0.12✓/Lycanites 0.1.0-alpha✓(ashenstone 全家在)/TerraBlender✓；**无 NTP/BOP 本体；ScalingHealth 功能已并入 `simple_difficulty-1.0.1c`(Simple Difficulty Reforge)**——含 `heart_crystal_ore`/`deepslate_heart_crystal_ore` 及 NTP 系 `*_loose_rock`×6（松石子为非实体装饰块，不能替全块路面））；②特种矿→**EPCA `infested_heavy_*` 虫染重矿**（thorium→emerald/lithium→redstone/lead→lapis/boron→iron），**crystal 系两条精确归位 simple_difficulty**：`contenttweaker:deepslate_crystal_ore`→`simple_difficulty:deepslate_heart_crystal_ore`（1:1 还原）、`scalinghealth:crystalore`→`simple_difficulty:heart_crystal_ore`；P1④ loot 阶段注意：宝箱里 scalinghealth 物品→`simple_difficulty` 的 heart_crystal/heart_shard/heart_dust；③血/肉→**本模组自建 `underneath:blood` 流体+`underneath:flesh`**，从 `E:\DefiledLands-1.20\BiomesOPlenty-1.20.1` 源码对照移植（**BOP 1.20.1 源码里 blood 流体/flesh/porous_flesh/blood_lake/blood_spring/visceral_heap 群系全都在**——中间版本删过后来又回来了；用户称有授权）。NTP 仅 1 方块 `cobblestone/andesite`(10.7万)→`minecraft:andesite`（不装 NTP）。
- **P1 已完成部分**：`blockmap/BlockMapper.java`（SimpleJsonResourceReloadListener 读 `data/<ns>/block_map/*.json`；原版 `BlockStateParser` 解析目标（支持 `[prop=v]`）；不可变 Map+volatile 换新（worldgen 多线程只读）；未映射回退**品红釉陶**占位+有界告警集 cap512；查询=精确→去 meta 基名→回退）+ `data/underneath/block_map/modded.json`（约 90 条手工映射，全部观测到的 mod token，含 TODO 观感项）。**架构要点：不加任何 compile 依赖**，全靠运行时注册表按名解析，缺 mod 自动回退。
- **P1① 血/肉自建已完成（待编译）**：`fluid/BloodFluid`（照 BOP 1.20.1 移植：tickDelay7/坡度3/不可无限/防爆100；**Forge 坑：模组流体必须重写 `getFluidType()` 否则运行时抛异常**，BOP 靠 MixinBloodFluid 补、我们直接重写）+ `init/ModFluids`（FluidType 密度3000/黏度6000/可灭火 + 客户端血贴图/水下暗红雾0.407,0.121,0.137 + **FluidInteractionRegistry：他液碰血凝成血肉/多孔血肉**，commonSetup enqueueWork 注册）+ `init/ModBlocks`（blood LiquidBlock/flesh/porous_flesh，珊瑚音效0.4硬度）+ `init/ModItems`（BlockItem×2+血桶，进原版创造栏）+ `block/FleshBlock`（踩上减速+剪刀刮肉→多孔+掉腐肉；**裁剪** BOP randomTick 增生/滴血粒子/环境音）+ ClientSetup 流体 translucent 渲染层；资源：blockstates/模型/双语 lang/流体 tag `underneath:blood`/loot 自掉落。**贴图已全部自绘替换（2026-09-12，`dev/paint_textures.py` value-noise 程序化像素画：血液缓涡/竖流纹动画、血肉三帧脉动+等值线筋络、多孔蚀孔、手绘血桶）——不再含任何 BOP 素材，协议上仅剩"行为逻辑参照 BOP 源码"；调色改脚本里 BLOOD/FLESH_BASE 色板重跑即可，预览产出 dev/texture_preview.png**。映射表 `underneath:blood/flesh` 预指现已落地、编译后告警自消。
- **包修复 mixin（2026-09-11·首个真 mixin）**：1.70 启动崩溃＝**BCLib 20.0.13 竞态**——`WorldConfig.MODS`(裸 ArrayList) 的 `registerModCache` contains+add 在 Forge 并行 common_setup 被 BCLib+BetterNether 同时调 → ArrayList 内部 AIOOBE"Index 1 out of bounds for length 0"（加入本 jar 只是改变调度时序引爆，本就每次启动掷骰子）。修复=`mixin/BCLibWorldConfigMixin`（`@Pseudo`+`targets`字符串→无需 BCLib 编译依赖、缺席静默跳过；HEAD+cancel 整体替换、MODS 监视器内 contains+add；registerModCache 是唯一写入口故完备）。已开真 mixins.json 列表——**refmap WARN 从此由 AP 正常生成消除**。诊断法：javap 反编包内 jar 的 class 拿字段名/描述符再下刀。
- **P1② legacy 转换表已完成（2026-09-12）**：`dev/gen_legacy_map.py`（生成器入库 dev/，可反复再生；改映射改生成器再重跑，勿手改 JSON）→ `block_map/legacy_vanilla.json` **1392 条**。观测真材料 **100% 覆盖（UNCOVERED=0）**，42 个假 token（SKYSCRAPER_*/LOOT/RLCRAFT 等路径碎片）用 OTG DefaultMaterial 白名单剔除。要点：Bukkit 名系（STEP=石台阶/WOOD=木板/SMOOTH_STAIRS=石砖楼梯/IRON_FENCE=铁栏杆/IRON_PLATE=重质压力板/RED_ROSE=各色小花）；方向 meta 语义已编码并抽查验证（楼梯 &3 朝向+&4 倒置、活板门 N/S/W/E、门下半 &3=E/S/W/N+上半 hinge、LOG &3 树种+>>2 轴向(3=全皮wood)、床 &8=head、拉杆 EnumOrientation、双台阶 8=无缝平滑石、树叶恒 persistent=true 防结构腐烂）；流体 STATIONARY_WATER:n→water[level]；OTG **PlantType 植物别名**（DANDELION/TALLGRASS/ROSEBUSH 等 19 个，来自 .bc Grass()/Plant() 行）一并收入，P2 直接复用。
- **P1③a BO 数据+解析器+L1 指令放置已实装（2026-09-12·待编译）**：
  - **数据入库**：`dev/pack_bo_data.py` 把 jar 内 2416 个 .bo3/.bo4 规范化（全小写/空格→下划线，适配 ResourceLocation）打进 `data/underneath/bo/`（剥注释后 **342MB 原文 → jar 内 deflate 实测 22.4MB**，可接受；对象名去后缀零冲突）。.nbt 方块实体 P1④ 再入。
  - **Java**：`bo/BOObject`（紧凑存储=BlockState 调色板+int[] 坐标/索引平行数组；BranchRef 记录待 L1.5/L2；vanilla 旋转语义 CW90:(x,z)→(-z,x)）、`bo/BOParser`（长短函数名两套；RandomBlock 的"材质[,nbt],几率"序列；分支宽松解析；方块 token 过 BlockMapper 调色板去重）、`bo/BORegistry`（**惰性解析**：reload 只建名→资源索引，首用 computeIfAbsent 解析缓存、reload 清缓存——342MB 不能全量预解析）、`command/UnderneathCommands`（`/underneath placebo <名> [none|cw90|cw180|ccw90]` OP 调试放置，tab 补全全部对象名）。
  - **验收**（重编后）：`/underneath placebo bloodspawner`（含随机方块的小 BO3）；tab 补全挑棵树（如 hugetaiga1）；放个尖刺切片 `ceiling_spike-diag-big-01_0x0`（会提示"分支暂未展开(L1.5)"属预期）。看方块/朝向/颜色是否正确（楼梯朝向错=legacy 表相应家族要修生成器）。注意 jar 变大 ~22MB、构建略变慢属预期。
- **P1③b L1.5 刚性分支扁平化已实装（2026-09-12·待编译）**：
  - **OTG 旋转语义已从源码钉死（`bo/BORotation` 集中承载，勿凭直觉改）**：枚举 **N0→W1→S2→E3**、合成=id 相加 mod4；分支偏移与 BO3 方块每步 `(x,z)→(z,−x)`（逆时针）；**BO4 方块 chunk 对齐每步 `(x,z)→(15−z,x)`（顺时针）**；方块态与所属坐标变换同向（若进游戏发现旋转件朝向反，翻转 `stateRotation` 一处即可）。BO3 的 `-1z` 怪癖函数只用于继承包围盒，与方块/分支无关。
  - **分支行格式（源码+全量普查）**：BO4=`(x,y,z,required,[名,旋转,几率,深度]×N[,totalChance][,branchGroup])` 四元组；BO3=三元组。**required 行只取第一节点恒 100%（OTG 同款）**。普查：2012 行分支/216 WBR；**required=true 占 1832 行**、单节点 1796 行→刚性覆盖 ~91%；非 NORTH 旋转 1082 节点（旋转合成必须做）；多节点池（2~31 选一）=城市随机选楼=L2。
  - **实装**：`BOObject` 分支重构为 BranchLine(x,y,z,required,weighted,totalChance,nodes)+`deterministic()` 判定；`BOParser` 修四元组解析+行尾 branchGroup 误吞 bug；`BOFlattener`（递归展开确定性分支→合并平坦方块列表；护栏=深度 24+预算 400 万块+truncated 标记；IntBuf 防对象海）；`BORegistry` 索引带 bo4 标记+扁平化缓存；`placebo` 指令默认放扁平化结果、旋转参数改 north/west/south/east（OTG 序），回显"拼装 N 片/随机分支未展开 M 行"。
  - **验收**（重编后）：①`/underneath placebo single_skyscraper_3x3_01`——**整栋摩天楼**应一次成型（拼装片数>1）；②`/underneath placebo single_skyscraper_3x3_01 west`——整体旋转后楼仍完整、楼梯/门朝向随转（朝向反=翻 stateRotation）；③`/underneath placebo citygrid`——只出网格骨架+提示大量"随机分支未展开(L2)"属预期；④小树 placebo 回归不坏。
- **P1④ 前置核对已完成（2026-09-12·扫全 37936 个 .nbt+BO Entity/Spawner 行）·三项拍板已定**：
  - **战利品表 96 引用**：`minecraft:chests/*`×14 全在（唯 `village_blacksmith`→`village/village_weaponsmith`）；`iceandfire:*`×5→**加 `chest/` 前缀即可**（IAF-RLC 1.20.1-2.2.3 在 1.70，路径 `iceandfire:chest/<名>` 已核实）；**`dregora:*`×60 → 从 `RLCD1.1.2.b/mods/DregoraRL-3.9.jar` 移植**（1.12 表在 `assets/dregora/loot_tables/`，共 552 个 json，只移被引用的 60+其内部嵌套引用；lore 系=成书）；**`charm:*`×17 → 直接移植**（已拆内容：全原版物料零 charm 私有物，jar=`RLCD1.1.2.b/mods/Charm-1.12.2-1.4.1.jar`；1.12→1.20 物名改：fish→cod/reeds→sugar_cane/wooden_door→oak_door/dye按meta/web→cobweb/generic spawn_egg 按 EntityTag 转具体蛋）；`quark:chests/pirate_chest`→`minecraft:chests/shipwreck_treasure`。
  - **实体（刷怪笼+Entity 行）**：vanilla 全在（唯 `vindication_illager`→`vindicator`）；**srparasites→EPCA 映射**：直配=rupter→ripper/incompleteform_*→*_incomplete_form/beckon_sii→stage_ii_beckon/carrier_flying→flying_carrier/movingflesh→living_flesh_size*/pri|ada_longarms→reshape_longarms/pri_yelloweye→reshape_yelloweye/sim_villager|wolf|enderman→infested_同名；**EPCA 无对应的 ~15 种（host/grunt/thrall/haunter/heed/crux/marauder/monarch/overseer/vigilante/wraith/bomber_*/carrier_colony/pri_reeker|bolster|summoner|arachnida/ada_*余/anc_dreadnaut/anc_overlord）→ 就近降级到 EPCA，映射文件里单列 `__substituted__` 分组标明原 id（用户要求：便于日后逐个换更贴切的）**。
  - **VC 装备 17 种（1.70 无 VariedCommodities）**：金币→金粒；crowbar/wrench/pipe_wrench/hammer/lead_pipe→斯巴达铁质锤/钉头锤系（1.70 在，注册名格式 `<材质>_warhammer/flanged_mace`，实装时核对）；infantry_helmet→铁盔；**soldier(军绿)/commissar(黑红)/nanorum(银灰)三套制服→染色皮甲**（display.color）；坏前缀 `ariedcommodities:` 同规则修复。**同样在映射里标明原 id**。
  - **TE 转换清单（④代码）**：banner×12656(TE Base 色→彩色旗帜方块+Patterns 保留)、bed×4296(color→彩床)、flower_pot×3806(Item/Data→potted_*，内容 9 种已扫出)、sign×3858(Text1-4→1.20 front_text)、daylight_detector×1240/noteblock×48(1.13 起无 TE→弃)、skull×26(SkullType→方块已映射)、容器类挂 LootTable/Items、charm:crate→barrel、quark:quark_chest→chest、其余 mod TE 弃。箱内直接物品仅 10 种（绝大多数走 LootTable）→小物品表即可。扫描器：`<scratchpad>/nbt_inventory.py`（最小 NBT 解析器，按标签语境分类）。
- **P1④a+b 已实装（2026-09-12·待编译）**：
  - **数据**：37936 个 .nbt 已随打包脚本入 `data/underneath/bo/`（含共享宝箱库 `bo/chests/**`；总 40352 文件 344.6MB raw）。**nbt 按全路径索引**（基名重复 4469 组不能按名）；Block 行 nbt 参数=相对其 BO 文件目录、支持 `../..` 上溯（`BORegistry.resolveNbtPath` 归一化+栈解析）。
  - **Java**：`blockmap/IdMapper`（LOOT/ENTITY/ITEM 三实例 reload listener；值可带 `{"id","tag"}` 附加 NBT（JsonOps→NbtOps）；`__substituted__` 分组=近似替代单列标明；未命中原样放行）；`bo/TileEntityConverter`（按**已放置方块**派发：旗帜(1.12 Base **色值反转** 15=白→彩色旗帜方块+Patterns 同反转)/床(color→彩床)/花盆(Item:Data→potted_*)/告示牌(Text1-4→front_text+waxed)/头颅(SkullType/Rot)/刷怪笼(SpawnData/Potentials→1.18+ 包裹格式+实体映射+装备映射+Passengers 递归)/容器(LootTable 含 charm 小写 lootTable+Items 逐项)；物品特例=skull 按 Damage、record_*→music_disc_*、旧 ench 丢弃）；BORegistry 加 nbt 索引+加载缓存（存 Optional 返回副本）；BOFlattener 穿线 nbt(blockIdx+全路径)；placebo 放置后逐块应用并回显"方块实体 N/M"。
  - **映射数据**：`loot_map/loot.json`（vanilla 改名+iceandfire 加 chest/ 前缀+dregora→underneath 同路径+charm→underneath:charm/*+quark 海盗→沉船）；`entity_map/entity.json`（直配 13 条+`__substituted__` 28 条降级替代）；`item_map/item.json`（arrow_explosive→explosive_arrow 名反转！+VC→斯巴达锤系/金粒/三套染色皮甲 5200433 军绿/3021338 黑红/11318717 银灰，含坏前缀 ariedcommodities 修复）。
- **P1④c 已完成（2026-09-12）——🎉 P1 全线收官**：`dev/port_loot_tables.py` 移植 **270 个表**（dregora 60+charm 17+嵌套递归）→ `data/underneath/loot_tables/**`，**零残余零无效产物**。流水线要点（改表改脚本重跑，勿手改产出 JSON）：
  - 待移植清单读 `loot_map/loot.json`（单一事实源）；1.12 宽松 JSON 用 strict=False+定点修复（city.json 的 `conditions":` 原包丢引号）；物品 lowercase（原包有 Diamond_axe 混写）。
  - **深链截断**：`dregora:rlcraft/loot_bundles/**`（RLCraft 武器海捆包，48 mod/455 物品，不整体移植）→ 按语义分级替代（potion→charm 药水表/golden|epic→epic_loot/rare|gem→rare_loot/其余→common_loot），全部记录在 `dev/loot_report.txt` 备注段。
  - **1.70 物品全集对撞**（缓存 `dev/pack_items_1.70.txt`，11592 项）：在场同名直传；**斯巴达词序自动反转**（1.12 `dagger_gold`→1.20 `golden_dagger`）+类型改名（hammer→battle_hammer/mace→flanged_mace/staff→quarterstaff/throwing_axe→tomahawk/throwing_knife→dagger/crossbow→heavy_crossbow/silver→iron 降级）；spartanfire 毒系→斯巴达钻石同类、龙骨系→iceandfire:dragonbone_sword；缺席 mod ~130 条逐条近似映射（脚本 MOD_ITEMS，兼当"标明原 id"清单）；**映射产物再过名单校验**防写错。
  - 重要事实（2026-09-12 用户补充后修正）：**1.70 直传命中**=aquaculture/roughtweaks(medkit 改名)/simple_difficulty/fishingmadebetter/waystones/firstaid/artifacts/xpbook/pigstep/locks/grapplemod；**换壳对应版（modid 变了、已重映射原味归位）**=BountifulBaubles→`bountifulcurios:`(下划线命名 trinketapple→apple 等 14 条)、FoodExpansion→`foodexpansion:`(1.10.0,去 item 前缀蛇形化 14 条)、更好生存→`bettersurvival:`(iaf-rlc 版,银系 5 件**原名只换 ns**,lumium→itemancientmetalhammer)、Rustic 酒庄物→`vinery:`(red_grape/dark_cherry_barrel/big_bottle)、NTP 金刀/锄→`simple_difficulty:gold_knife/gold_mattock`；**真缺席**=VariedCommodities/mod_lavacow(Fish's Undead Rising)/armorunder/qualitytools/xat 等杂项（**VC 协议已查证：CC BY-NC 3.0**）；
  - **VC 物品移植已完成（2026-09-13）**：41 种真身入本模组（`dev/port_vc_items.py` 提贴图/生成模型/合并 lang；盔甲层贴图 `_1→_layer_1` 落 `textures/models/armor/`）。Java：`items/UnderneathTiers`(BRONZE/MITHRIL/EXCALIBUR)+`items/UnderneathArmorMaterials`(soldier/commissar/nanorum/infantry/mithril 五档，**getName() 带 `underneath:` 前缀——Forge getArmorResource 支持 ns 形式定位穿戴层贴图**)+ModItems 批量注册（helper 压缩：19 武器 SwordItem 近似档数值/14 盔甲/8 杂项；**soldier/commissar 的 bottom 风衣=BOOTS 槽**补齐四件）。无配方（战利品获取）。两处映射已回指真身：loot 脚本 22 条+item_map 怪物装备（染色皮甲方案废弃）；loot 已重跑零残余。**署名已落 mods.toml credits**（CC BY-NC 3.0 署名 Noppes+非背书声明+Dregora 原作者致谢）。数值为近似档，微调改 ModItems/两个材质类。`aquaculture:loot`→`neptunes_bounty`；`biomesoplenty:fleshchunk`→`underneath:flesh`（自家的）；味道损失最大待细调=lavacow boss 武器（reapers_scythe→钻石锄等）。
- **P1 首轮实测三修（2026-09-13·用户实测反馈）**：
  - **全空箱**根因=移植表残留 1.20 非法构件，**一个坏函数废整张表**：`set_attributes` 1.12 驼峰属性名×573（主犯）/`set_data`×153（mod 物品分支漏剥）/`rlmixins:enchant_specific`×40。修=`clean_functions` 统一出口清洗（item/loot_table/pool 三级全挂）：驼峰→蛇形、set_data 剥、enchant_specific→`set_nbt Enchantments` 等价转换；顺带 **display.Name/CustomName 纯文本→JSON 文本**（不修则 Dregora 风味命名全丢）、`rlmixins:curse_break` 药水→浓再生（保留风味名）、LocName 清除。270 表已重生成。
  - **全白旗**根因=**`sendBlockUpdated` 不携带 BE 数据**——服务端已是彩旗+图案，已加载区块的客户端看到空数据白旗。修=`loadInto` 显式 `be.getUpdatePacket()` 发给 `chunkMap.getPlayers(chunkPos,false)`。**任何运行时改 BE 后要客户端立现，都必须补发 BE 数据包**（本项目通用坑）。
  - **城市笼出寄生虫=非 bug**：`cityspawners.bo3` 本体就是 `Parasites/parasite_multi.nbt`（1.12 原版即寄生虫笼）；军装僵尸笼在 `citychestfiller_*` 系。
  - 诊断数据：nbt 路径解析命中率 92%（2715/2942），227 处失败=chests 库互引 `../Lore/*`（**原包即断链**，OTG 同样加载不到，无需修）。banner nbt 实证含 Base+Patterns 且色值转换逻辑核对无误。
  - **第二轮"依旧复现"真相（2026-09-13）**：用户重测跑的是 **Sep 12 旧 jar**（三修后未重新构建/未覆盖——构建产物与包内 jar 时间戳字节数完全一致且为修复前版本）。**教训：每轮修复后必须核对 build/libs jar 时间戳已更新并覆盖进包再测**。另两项实锤：`all_loot_test`/`loottest` 系是 Dregora 作者**原包即断链的废测试对象**（引用 `../Lore/`→不存在的 bo/lore/、`bone_loot_1.nbt` 等不存在的文件名），勿用作测试点——**正确开箱测试点=`loot_misc`(5箱)/`loot_ancient`(lore成书)/`loot_epic`**；`citychestfiller_*` 也是 parasite_multi 寄生虫笼（城市笼族全员寄生虫=1.12 原设定，映射行为正确，军装 zombie 混编在多重池内无独立测试对象）。
- **P1 二轮三修+附魔体系大修（2026-09-13）**：
  - **二轮"依旧"真相=跑了旧 jar**（构建产物未覆盖进包；已代覆盖并留验证法：核对 build/libs 与包内 jar 时间戳）。`all_loot_test`/`loottest`=原包断链废对象勿测（正确测试点 `loot_misc`/`loot_ancient`/`loot_epic`）；`citychestfiller_*` 同为寄生虫笼（原设定）；军装怪 spawner（`spawners/maintenance.nbt`）是**原包孤儿文件无引用**——要看军装怪用 summon 带 `underneath:soldier_*` 装备，或日后 P2 接线。
  - **阴险 bug：rlmixins→set_nbt 转换产物不再过同循环的 set_nbt 清洗**（分支顺序死锁）——源里 Dregora 传奇武器附魔全走 `rlmixins:enchant_specific`，故 charm/somany 附魔穿透清洗入表。修=统一 `map_enchant()`（rlmixins 转换/set_nbt 正则逐项/enchant_randomly 三处共用），**转换时先映射再生成**。诊断法=对 main 流程打探针（单元测试测不出流程序 bug）。
  - **SME→jlme 附魔迁移（用户情报：`jlme-1.20.1forge-ver1.9b` 是 SoManyEnchantments 替代）**：jlme 77 附魔=SME 连写名改蛇形（`advancedknockback→advanced_knockback`）→ 运行时从 jlme lang 建"去下划线反查表"自动配 17 个 + 手工语义配对 31 个（magmawalker→lava_strider/supremebaneofarthropods→supreme_ba/rune 系同名蛇形/innerberserk→double_edge 等，见脚本 ENCH_MANUAL）+ 纯诅咒系 10 种无对应丢弃；bettersurvival 附魔只换 ns。
  - **SpartanFire 1.20 命名空间是 `spartanfire_rlc`**（之前物品全集 grep 前缀漏 `_rlc` 误判为空）：八大族×24 类型全在（dragonbone→dragon_bone、fire→flamed_/ice→iced_/lightning→lightning_dragon_bone、毒系→desert/jungle_myrmex_stinger、staff→quarterstaff/hammer→battle_hammer 等类型改名）——spartanfire 全系已**精确映射真身**（52 处落表），替代降级作废。
  - **风味修复**：display.Lore 数组同 Name JSON 化+LocLore 清（残留 8 处为 MC 忽略的未知键，无害）；**招牌武器「Megumin's Staff/惠惠的法杖」**（`unique_weapons_epic`，火龙骨长杖+束缚/消失诅咒+jlme 易伤/处决/暴击/灰烬/高火；爆裂魔法由 `simple_difficulty:burst` 涂药承担——1.20 港版 bettersurvival 无 blast 附魔，已映射为击退）。
  - **P1 涂药彩蛋修复（2026-09-13，纯数据零 Java）**：「武器涂药」命中挂效果机制＝**更好生存** `CommonEventHandler#onLivingAttack`（读武器 `{Potion:"<药水类型>",remainingPotionHits:N}`，用 `PotionUtils.getPotion` 按【药水类型】解析后 `potion.getEffects()` 逐条施加——**目标必须是真实注册的药水类型，MobEffect/CustomPotionEffects 都不算**）。故只需在 `dev/port_loot_tables.py` 的 `POTION_MAP` 把断链药水 id 重指到 1.70 现存药水：potioncore:explode→`simple_difficulty:burst`(惠惠命中爆炸)/launch→launch/lightning→lightning/fire·strong_fire→fire/strong_teleport→random_teleport；srparasites:viral→`minecraft:poison`、repel→`simple_difficulty:recoil`；mod_lavacow:soiled→slowness；lycanitesmobs:smited→`simple_difficulty:fire`(0.1.0-alpha 无药水类型)；`minecraft:blindness`(原版无致盲药水，Heaven's Blade 本就一直失效)→`bettersurvival:blindness`；旧 ns `mujmajnkraftsbettersurvival:milk`→`bettersurvival:milk`(×8)；xat 矮人变身药水(Trinkets&Baubles，无对应)→力量药水。另修 `potioncore.digSpeed`(非法 ResourceLocation 大写 S，会废掉整张 unique_weapons_rare)→在 set_attributes 里丢弃该 modifier。改动仅 6 表，其余 264 表重跑逐字节不变。
  - **P1 彩蛋武器三修（2026-09-13·"箱子开不到"根因）**：①**附魔注册名全量校验**——`enchant_randomly` 未知 id 在**解析期废整张表**（unique_weapons_epic 就是这样整表阵亡的，权重再高也开不到）。从 1.70 全部 jar 的 lang 收割附魔注册名比对，补 ENCH_MANUAL 13 条：bettersurvival:blast/fling→击退、penetration→jlme:armor_piercing、smelting·simpledifficulty:heating→jlme:heating、charm:homing·spartanweaponry:return→忠诚、defiledlands:destructive→锋利、sharpshooter·spartanweaponry:propulsion/supercharge→力量、incendiary→jlme:advanced_fire_aspect、rapid_load→quick_charge；**并修 map_enchant 早返回 bug**（mujmajnkrafts→bettersurvival 换 ns 后直接 return 绕过了 ENCH_MANUAL 二次映射）。裸名附魔(`sharpness`)默认 minecraft: 属合法勿动。②**多条 set_nbt {Enchantments:[单附魔]} 合并为一条**——1.20 SetNbtFunction 走 CompoundTag.merge，**列表键整体覆盖**，多条只活最后一条、排在 enchant_randomly 后还会抹掉随机附魔；clean_functions 尾部后处理：抽出无条件纯 Enchantments 条目按 id 去重(取高等级)合成一条，插到首原位与首个 enchant_randomly 之前。③**全家桶箱（二次拍板：原表复原）**：epic/rare/moderncity 4 张原表已**复原 Dregora 原始权重**（epic 2/18≈11%、rare 2/68，保底池方案作废）；改为生成器从转换后的 unique_weapons_epic **派生** `loot/unique_loot_epic/all_unique_weapons.json`——每条目独立 rolls=1 池，**一箱必出全部 24 件**（3 捆包+21 具名武器，≤27 格装得下），武器改动自动同步。取用：`/setblock ~ ~ ~ minecraft:chest{LootTable:"underneath:loot/unique_loot_epic/all_unique_weapons"}` 或 `/loot give @s loot <同id>`。验证=全表附魔复检 PASS+defiledlands 残留 7 处均为合法物品引用。
  - **P1 撇号大修（2026-09-13·37 表整表阵亡的最终真凶·从游戏日志实锤）**：脚本把 Name/Lore 包成**单引号 SNBT** `'{"text":"..."}'` 却没转义文本内嵌撇号——"Megumin's/Gandalf's/There's/Khaldir's" 直接截断字符串 → `缺少"}"` 整表解析失败（79 处坏 tag/37 表：unique_weapons_epic、gilded_sword 系、special_potion 系、全部 lore 成书的 Lore 行、moderncity/oceanruin 系）。修=两处包裹点 `.replace("'","\\'")`。**同批清缴**：LocName 删除残留前导逗号 `display:{,`（3 张骨头表`需要值`报错）→ 清扫规则加 `{,`/`,}`；`generic.reachDistance` 驼峰漏网（demonic_staff 阵亡）→ `forge:entity_reach`；**STALE_FIX 名单陈旧覆盖表**（pack_items_1.70.txt 里有但运行时已死，置于直传前+产物再过一遍）：`quark:root`→hanging_roots、`simple_difficulty:purified_water_bottle`→同mod bucket、`vinery:big_bottle`→wine_bottle(1.4.41 改名)；`golden_rail`→powered_rail；spawn_egg 实体改名表(vindication_illager→vindicator 等 6 条)。**验证方法论（下轮沿用）**：①自写 SNBT 良构扫描器过全部 set_nbt tag；②物品名单三方校验=原版 1.20.1 client.jar lang 收割+包名单(-陈旧项)+ModItems 注册名；③属性白名单——三项全零才算过。**教训：lang/物品清单都可能陈旧，最终裁判是游戏日志 `Couldn't parse element`（注意各表 fail-fast 只报第一个错，修完必看新日志）。**
  - **P1 隐藏力量复活（2026-09-13·用户记忆点：1.12 彩蛋物用 HideFlags 藏附魔/效果/修饰符）**：审计结论——`HideFlags` 251 处原样存活且 1.20 语义同（1=藏附魔/2=藏修饰符/32=藏药水效果）；但 **1.12 数字附魔 `ench:[{id:N,lvl:M}]` 1.20 静默忽略**（15 表 669 条隐藏附魔在丢失：cursed_armor/weapons/tools、epic_3/4 神装、gilded_sword、fish_undead 等）→ 加 `ENCH_NUMERIC_112` 30 条原版数字 id 表转成 `Enchantments:[{id:"minecraft:x",lvl:Ns}]`（观测 id 全在原版域，仅 id=100 模组动态 id 丢弃并报告）；`CustomPotionEffects` 数字 Id 1~27 与 1.20.1 原版一致**本就生效**（诅咒药水的隐藏效果一直是活的），仅 Id=72 模组效果指错对象→丢弃；NBT 版 AttributeModifiers 全项目 0 处（修饰符全走 set_attributes 函数已修）。三维复检（SNBT/物品/属性）三零通过。
- **P1 整体验收（最新版·重编后一次跑全）**：①启动日志三行（BlockMapper ~1480 条/BORegistry 2416+37936/IdMapper 三表）；②`/underneath placebo loot_misc`+`loot_ancient`+`loot_epic`——开箱有货、lore 成书、传奇武器带 jlme 附魔+风味名（如「Megumin's Staff」火龙骨长杖），**回报"方块实体 N/M"数字**；③`/underneath placebo single_skyscraper_3x3_01`——旗帜**放置即显**彩色图案、床/花盆/告示牌正确；④创造栏战斗页 33 件 VC 武器盔甲、穿政委套看渲染；⑤军装怪 summon 验证（`/summon minecraft:husk ~ ~ ~ {ArmorItems:[{id:"underneath:soldier_bottom",Count:1b},{id:"underneath:soldier_legs",Count:1b},{id:"underneath:soldier_chest",Count:1b},{id:"underneath:soldier_head",Count:1b}],HandItems:[{id:"underneath:pipe_wrench",Count:1b},{}]}`）；⑥血/肉自绘贴图观感+水碰血凝肉+剪刀刮肉。
- **P1 完成后 → P2**：群系+表层(SurfaceRules)+矿物+原生地下地形调参（材料：`.bc` 表层/矿物行已在 un_extract 分析过，legacy 表含 PlantType 植物名可直接复用）。

## 八、P2 进行中（群系+表层+矿物+地下地形）

**P2 起步·`.bc` 全量参数提取已完成（2026-09-13）**：工具 `dev/extract_bc.py`（可复跑，源在 `E--EnigmaticEchoes-1-20-1/.../scratchpad/un_extract/.../WorldBiomes`）→ `dev/bc_inventory.tsv`。关键结论：
- **83 个 .bc = 12 顶层主群系 + Fromimage 变体**（Ancient Remnants/Remnants Edge/Wall/Outter Edge，R-ONE~FIVE 是同参旋转变体=map.png 分区用）。
- **地形几乎全同**：12 群系统统 `BiomeHeight=0.9 / BiomeVolatility=-2.0 / Vol1=Vol2=2.0 / w1=1.5 w2=1.45 / MaxAvgH=MaxAvgD=0`——**只有一套地下型地形剖面**，靠 `CustomHeightControl`(33 段浮点) 塑造"顶盖+地板"的洞穴腔体（Underneath.bc 剖面：底部高→中段深挖→缓收）。→ **P2 核心难点=把 CustomHeightControl 翻成 1.20 density_function/noise_settings**（不是做 12 套噪声）。
- **方块**：Stone/Surface/Ground 基本全 `eaglemixins:deepslate`→原版深板岩（唯 Defiled Caverns=污秽石）；表层 `SurfaceAndGroundControl` 按噪声掺 basalt/寄生碎石带（→SurfaceRules 的噪声条带）；`ReplacedBlocks` 把 water/blood 按 Y 挖空成 air+lava→basalt。
- **矿物**：每群系 ~12 条，去向全部已拍板（原版深板岩矿 + EPCA 虫染重矿 thorium/lithium/lead/boron + simple_difficulty 心水晶）→ P2 做 configured/placed_feature。
- **液体**：blood 湖两条 Y 带（8-60 / 140-255）→ underneath:blood。
- **颜色分两系**（群系区分主要靠颜色，非地形）：核心红雾系(sky#130101/fog#2e0303 dens0.2/grass#2e1412/foliage#4d2406)；Edge/Pillar 蓝过渡系(sky#11205f/grass#c8c5ab)。
- **刷怪 `SpawnMonsters: []` 全空**→不做 biome spawner（靠维度默认+城市寄生虫笼结构，P3）。
- **待补 BlockMapper 的新 token**（P2 顺手）：`concrete:7`→gray_concrete、`dimstack:bedrock:0`→bedrock、`nuclearcraft:fluid_corium`→lava(待定)、`srparasites:parasiterubbledense:1`→epca、`quark:crystal:7`→待定；1.12 裸名(stone:1/3/5、coal_ore 等)legacy 表已覆盖。
- **P2 建议施工序**：①CustomHeightControl→noise_settings/density（先在占位维度调出地下腔体手感）②核心 biome JSON(先 1 个 Underneath 红雾系跑通)③SurfaceRules(deepslate 基底+basalt/碎石噪声带)④矿物 features⑤植被 Feature(挂 P1 的 BO 树)⑥Edge/其余群系批量+BiomeSource(先固定/多群系 checkerboard，map.png 分区可选后置)。

**P2① 地形密度已实装（2026-09-13·待编译验证）——OTG 密度公式已从源码逐行钉死**：
- **数学（ChunkProviderOTG#generateTerrainNoise，重看必读）**：`密度 = 体积噪声(vol1Noise/512×Volatility, ±~128) + columnHeight + CHC[y]`；`columnHeight=(heightFactor−y)×12×128/heightCap/volatilityFactor`（正值再×4，顶 3 层强制衰减到 −10）；`heightFactor=33段×(2+heightSum+0.2×noiseHeight)/4`，`heightSum=(BiomeHeight×4−1)/8`；**关键：volatilityFactor=BiomeVolatility×0.9+0.1=−1.7 为负 → 梯度反转=下空上实的"倒置世界"**，这就是 Underneath 有穹顶的秘密。CHC index0=底层、每层 8 格。
- **算出的剖面**（OTG 单位，除以 128 → vanilla 密度域）：Y0-16 地板(+932→+26)/Y16-32 下碎洞带(−100)/Y32-40 边界砾/Y40-64 实心中层板(+50~+253)/**Y64-200 大空腔(−59→−255→−16，高136格)**/Y196-248 穹顶(+8→+129)/Y248+ 顶缝虚空。噪声 ±100 与偏置同量级→浮岛/石笋/破顶洞天然涌现。
- **实装文件（纯数据包零 Java）**：`worldgen/density_function/y.json`(y_clamped_gradient −64..320)+`base_bias.json`(36 点 spline=剖面/128)+`worldgen/noise/terrain.json`(firstOctave−7, amp[1,1,1,0.7])+`worldgen/noise_settings/underneath.json`(final_density=interpolated(base_bias+noise×0.9)、cell 1×2 对齐 OTG 4×8 网格、default=deepslate、aquifer/ore_vein 关、sea_level=1 暂无水——OTG 用 ReplacedBlocks 抽干 Y10-50，水环 Y50-63/血湖留 P2④、基岩底 −64)；`dimension/underneath.json` settings→`underneath:underneath`。
- **验收（重编后）**：进维度应见——脚下深板岩地板、抬头 Y~200+ 有穹顶（黑天+红雾下远观为暗）、Y64-200 大空腔可飞行穿越、Y40-64 有实心中层板、边界带浮岛/石笋。手感不对调参：腔体太碎→noise_settings 里 mul 0.9 调小；洞太圆滑→terrain.json 加高频 octave；剖面整体升降→base_bias 对应 Y 点值。
- **P2① 已知偏差待后续步骤补**：无水(原 Y50-63 水环)、无血湖、表层无 basalt/寄生碎石带(P2③)、biome 仍为占位 dripstone_caves(P2②)。**P2① 地形已用户实测通过（2026-09-13"地形正确"）**。

**P2② 群系已实装（2026-09-13·待编译验证）**：
- `dev/gen_biomes.py`（数据驱动生成器，改群系认知改它重跑）→ `worldgen/biome/` **12 个 biome JSON**（.bc 颜色/温湿直读：暗红主系 underneath sky#2F0000、红雾系 corrupted/underspikes/underforest/defiled_caverns fog#2e0303、蓝过渡 edge 系 #11205f、灰红 pillar 系 fog#26262E；`has_precipitation:false`、**空刷怪表=忠实 .bc `SpawnMonsters:[]`**、features 空待 P2④⑤、mood=ambient.cave）+ lang 合并（en/zh 各 12 群系名：地渊/腐化之地/污秽洞窟/地底森林/地底尖刺+边缘/石柱/空洞，merge 不覆盖既有键）。
- noise router `temperature/vegetation` 从 0 改为 `minecraft:temperature/vegetation` 噪声(xz_scale 3→气候斑块~340格)；`dimension/underneath.json` biome_source 由 fixed dripstone_caves → **multi_noise 5 核心群系**（underneath 占中央大盒 [-0.45,0.45]²、四家族各踞温湿四角）。Edge/Pillar/Hole 7 个变体 JSON 已生成但**暂未入分布**（原版靠 map.png 画边界，multi_noise 做不了描边——P2⑥ 或自定义 BiomeSource 时接入）。
- **验收（重编后新区块）**：F3 看 biome 应为 underneath:* 且随移动切换 5 群系；各群系雾色/水色/草叶色随之变化（underneath 黑雾、corrupted 红雾 0.2、defiled_caverns 同红雾）；`/locate biome underneath:corrupted` 可定位；无自然刷怪（原设定）。

**P2③ SurfaceRules 已实装（2026-09-13·待编译验证）**：
- **OTG 语义（重看必读）**：`SurfaceAndGroundControl`=(surface,ground,maxNoise) 三元组序列，列噪声(biomeBlocksNoise，值域~±2.75)从左到右取第一个 `noise≤maxNoise` 的层；1.12 genTerrainBlocks 对**每个 air→solid 朝上界面**都生效（腔体是密度生成非 carver，故地板/中层板/浮岛顶面全有表层，穹顶底面朝下=裸 deepslate）→ 恰好等价 1.20 `stone_depth surface_type:floor`。
- **条带（全 12 群系两端相同）**：noise≤-1.7 与 0.35-0.7 与 0.9-1.0→`quark:basalt`→**smooth_basalt**；-1.7~-0.7 与 0.7-0.9→`parasiterubble`→**epca:infested_stone**（与 P1 block_map 既定一致）；中段 -0.7~0.35 分三组：主系 10 群系=deepslate(走 default 兜底不写规则)、**Underforest=草方块/泥土**、**Defiled Caverns=defiledlands:grass_defiled/dirt_defiled**（jar blockstates 实锤 1.20 port 保留 1.12 名）。两特殊系叠 **ReplacedBlocks 的 Y 分层**：草土仅 **Y63-150**（主腔地板带），上下→gravel/gravel_defiled。
- **实装**：noise_settings surface_rule 三级 sequence=①bedrock 底 ②floor 面(特殊两系草皮，含 y_above 分层) ③floor+add_surface_depth(噪声条带，`minecraft:surface` 噪声、**OTG 阈值÷2.75 缩放**：±带界 -0.62/-0.255/0.127/0.255/0.327/0.364)。
- **⚠️ 硬依赖风险已知**：surface_rule 方块解析期硬校验——**无 EPCA/DefiledLands 的环境（如裸 runClient）整个 noise_settings 拒载维度崩**；本模组仅服务 1.70（两者都在✓），如需独立兼容再改分层数据包或运行时注入。
- **验收（重编后新区块）**：地板/浮岛/中层板顶面应现**平滑玄武岩带+虫染石带**穿插 deepslate；Underforest 主腔地板(Y63-150)有**草皮带**、腔外同噪声带变沙砾；Defiled Caverns 同理为污秽草/污秽砾；穹顶底面仍裸 deepslate（原版行为）。条带太宽/太窄→调 ③ 中阈值（整体×系数）。

**P2①-fix 地形噪声换 OTG 原语义（2026-09-13·用户反馈"空腔和分层没了"后返修）**：
- **诊断**：base_bias 剖面 spline 与 OTG 公式**逐点对拍零偏差**（曲线没错）；坏在两处——①v1 自配 4 倍频噪声频谱/振幅远离 OTG 的 16 倍频 beta 噪声（噪声项失真→腔壁/分层观感烂）；②P2② 换自定义群系时把 dripstone 占位自带的**洞穴雕刻器弄丢**（OTG CaveRarity7/Freq40 本就开洞穴，分层间隧道全没了）。
- **修复**：噪声项换 **`minecraft:old_blended_noise`**（Mojang 官方在 1.18 移植的 1.12 beta 三重插值噪声=OTG vol1/vol2/interp 原型；参数取原版 legacy 值 0.25/0.125/80/160/8，引擎对该类型自动接世界种子）**×2.0=Volatility**；全群系补 carvers `cave/cave_extra_underground/canyon`；删废弃 `worldgen/noise/terrain.json`。观感微调只动 ×2.0（1.5~3）。**若仍不达标→升级方案：Java 自定义 DensityFunction 1:1 移植 OTG generateTerrainNoise（含 2D noiseHeight 项），公式已解码在 P2① 记录里。**

**P2-OTG 终极方案：地形 1:1 Java 移植 + map.png FromImage（2026-09-13·用户拍板"忠诚移植"·待编译验证）**：
- **地形（数据近似路线作废）**：`otg/OTGLegacyNoise`（1.12 NoiseGeneratorPerlin(Octaves) 逐语义点采样移植：16777216 包裹、legacy 频率×d3/振幅÷d3 阶梯、**2D 首角 func_4110_a 怪癖**原样保留；纯 Java 零 MC 依赖）+ `otg/OTGTerrainMath`（全公式：2D noiseHeight 归一化管线→heightFactor→columnHeight(volatilityFactor=−1.7 反转,正值×4)→vol1/vol2 按 interp 选择(w1.5/w1.45)→顶3段衰减→CHC 分段线性；**尺度=684.412×2.1 横 / ÷3 纵（FractureHorizontal 1.1/Vertical −2——纵向压扁=横向撕裂感，此前所有近似丢失的关键性格）**）。**离线 javac 剖面已自证**：地板100%/下碎洞32%/中层板87-100%/大空腔0%/穹顶渐变+**穹顶垂悬巨石笋**全部涌现（测试法：scratchpad 编译两个纯类+SliceMain 打 ASCII 剖面——地形改动必先离线跑这个再进游戏）。
- **引擎接线**：`worldgen/OTGTerrainDensity`(DF 注册名 `underneath:otg_terrain`；JSON 包 minecraft:interpolated=引擎 4×8 角点采样恰为 OTG 原网格) + **种子握手（零 mixin·首测崩溃后改）**：~~RandomState 构造 HEAD mixin~~——**Mixin 禁止构造器 @At("HEAD") 注入**（`Found @Inject targetting a constructor` 崩服，构造器只许 TAIL/RETURN 而彼时接线已完成）→ 改 `worldgen/WorldSeedCapture` 静态 volatile + `ServerAboutToStartEvent`（早于一切维度/RandomState 创建，全服各维度共享同一种子）→ DF.mapAll 取种子生成带种子实例（NoiseHolder 通道只给 NormalNoise 载不动 legacy 噪声栈）。noise_settings final_density→interpolated(otg_terrain)、initial_density→0；旧 y/base_bias DF 与自配噪声全删。**教训：想在构造器早期注入的场景，先找 Forge 生命周期事件替代。**
- **map.png FromImage**：`worldgen/FromImageBiomeSource`(注册名 `underneath:from_image`)——OTG LayerFromImage 语义 1:1：**图层在 Voronoi 缩放之下 → 1 像素=4×4 格=1.20 quart 坐标零换算**；像素=quart−offset(−1500)、图外/未知色→fallback(既有 multi_noise=Normal 模式近似)；图惰性载自 jar `data/underneath/worldgen_image/map.png`(120KB→字节索引 9MB 内存)。**离线像素普查实锤设计**：map.png 94.25% 纯黑(→fallback)+5.75% 恰好=9 遗迹色全在色表——原图只画中央远古遗迹群(覆盖 ±6000 格)，六核心群系共用 #686297 因为花色分布本就交给 Normal 模式。
- **Remnants 9 群系**入 gen_biomes(共 21 biome：ancient_remnants+r1~r5/remnants_edge/wall/outer_edge，中文名远古遗迹·壹~伍等；矿物=裸 1.12 名已补映射 coal_ore→deepslate 系/stone:1|3|5→花岗闪长安山；植被暂灰岩柱+传送门废墟，遗迹结构 P3)。注册：`init/ModWorldgenTypes`(DENSITY_FUNCTION_TYPE+BIOME_SOURCE DeferredRegister)。
- **运行时探针**：`/underneath density`——采样当前维度真实 finalDensity 列打印实/空分段（数据↔引擎接线的最终裁判，diagnose 地形必先跑它）。
- 校验：全 JSON/引用/真环/维度 21 群系闭合 ALL PASS；map.png 色表覆盖 100%（黑=设计内 fallback）。**首次编译风险点**：RandomState mixin ctor 签名、DensityFunction.SimpleFunction/KeyDispatchDataCodec 用法、BiomeSource 抽象方法名——报错发全文即修。

**P2-OTG bug 修复：去掉 `interpolated` 包装（2026-09-13·"新世界仍实心"实锤后）**：
- **诊断铁证**：`/underneath density` 探针输出 `实[0-8]空[8-40]实[40-60]空[60-208]实[208-248]`＝引擎 finalDensity **确有 148 格大空腔**；离线 3 种子跑遍 3D 密度也全部干净空腔（噪声实测幅度仅 ±0.9 vanilla 域，不足以填腔）。**但新世界方块仍实心**→放方块没遵循 finalDensity。
- **根因**：探针读的是**未插值原始函数**（干净），放方块走 `minecraft:interpolated` 包装后的 NoiseChunk 采样——**唯一差别就是这层**，它按 4×8 网格插值自定义 DF 时把空腔填实（自定义 SimpleFunction 不适配引擎插值器的一个坑）。
- **修复**：noise_settings `final_density` 从 `interpolated(otg_terrain)` 改为**裸 `underneath:otg_terrain`**（引擎逐方块直调=与探针同路径=已证明干净）；`initial_density_without_jaggedness` 同改裸函数。**代价：逐方块 16 倍频噪声较慢**（~50-100ms/chunk，OTG 本就慢），观感确认后再优化（per-column 缓存 noiseHeight 2D 项）。**教训：自定义密度函数别套 interpolated，用 `/underneath density` 探针对拍原始 vs 实际是区分数学 bug 与引擎接线 bug 的唯一手段。**

**P2-OTG 地形已用户实测通过（2026-09-13"大体地形对了"）——种子 mixin 修复奏效。血湖改血海（同日）**：spring feature 只出渗流不出湖 → 改 `default_fluid=underneath:blood`+`sea_level=33`，底部下空腔(剖面 Y16-32)在海平面下自动灌血=底部血海（sea_level 可调：更浅↓/漫到中层↑）；上层 Y140-255 血由现有 spring 承担。**P2 至此实质完成**（地形/群系/表层/矿物/植被/血）。剩：ashen 灰烬石形态用户要求已移除(依附检查做好 P4 再正确重加)、血海 sea_level 观感微调、水环 Y50-63(可选)。**下一步 P0b 传送门 或 P3 城市（用户拍板）**。

**P2-OTG 真根因锁定：世界生成用未接种子的密度函数（2026-09-13·双路探针实锤）**：
- **铁证**：`/underneath density` 双路输出——密度行 `实[-64~16]空[16~32]实[32~64]空[64~168]实[168~252]`（有空腔）VS 方块行 `实[-64~120]空[120~124]实[124~312]`（几乎全实心到312）。**两行毫不相干＝放方块无视 finalDensity**。全实心正是 `compute` 里 `math==null→返回1.0` 兜底的特征：**世界生成时 OTGTerrainDensity 没接到种子**（探针那个 randomState 接了，所以显示空腔——两个不同 randomState）。
- **根因**：`ServerAboutToStartEvent` 捕获种子**晚于**世界生成用的 RandomState 创建 → 那个 RandomState 的 mapAll 接线时 `WorldSeedCapture.get()==null` → math 保持 null → compute 全返回 1.0 实心。
- **修复**：`mixin/RandomStateSeedMixin` 注入 **`RandomState.create(NoiseGeneratorSettings,HolderGetter,long)` 静态工厂 HEAD**（javap 得 SRG=m_255302_，ChunkMap 生成走此重载），在 new RandomState→构造器 mapAll 之前写 seed → 每个 RandomState 接线时种子必就位。**构造器 HEAD 注入非法(曾崩)，故注静态 create**。WorldSeedCapture 加 public set，事件保留兜底。**验收标准：双路探针两行应一致（方块行也出现 空[64~168]）**。
- **教训（本项目重大坑）**：自定义 DensityFunction 需要世界种子时，别靠生命周期事件（时机晚于 worldgen 的 RandomState）——必须在 RandomState 创建点（静态 create）注入。诊断靠双路探针对拍函数 vs 真实方块。

**P2-OTG 地形仍实心·双路诊断已加（2026-09-13·用户坚称新存档新区块=排除旧区块）**：去 interpolated 后探针仍空腔、方块仍实心，jar 已确认重建（含 otg_terrain 无 interpolated）。**盲点**：`/underneath density` 只读密度函数意见非硬盘真实方块。**已把指令改双路**：同列并排打【密度@x,z】(finalDensity)+【方块】(level.getBlockState 真实方块)，两行一致=地形对(漆黑看不见)/矛盾=放方块无视密度=真 gen bug；扫描范围改 Y-64..320。`OTGTerrainDensity.compute` 加一次性日志：抓生成时腔体高度(Y100-140)的坐标+返回值，或打印"未接种子(math=null)→兜底1.0实心"（若现此行=种子接线在生成线程失败=真因，ServerAboutToStartEvent 或早于某些维度 RandomState 创建）。**下一步等用户重建后发【两行输出】+【日志 OTG compute 行】定案。**

**P3 历史简记（2026-09-13·多轮已作废路线，教训保留）**：先后走过 ①CityExpander BFS+原版 Structure/RandomSpread（spacing 反复调参：24→10→14→18；spawner_city structure_set 7→24）②自创 CityPlotter GRID 网格布点（8→40）——**全部作废**（RandomSpread 无碰撞必重叠或留大缝；GRID 网格无空地扫描/无回滚=分布不忠实）。存留的有效教训：**城市不能扁平成一份方块列表（爆内存），必须逐零件按 chunk 放**；**worldgen 套方块实体=TileEntityConverter 泛化 LevelAccessor+instanceof ServerLevel 守卫补包**（城市宝箱/刷怪笼/旗帜由此有货）；**加新结构前先扫 mod 方块 token 过 block_map，未映射=品红粉点**；血泉第三带 Y64-100 count40（城市层渗血）；required-WBR 解析期短路砍候选和 RandomBlock 候选 nbt 被 i++ 丢弃两个解析 bug 已在当时修复（`if(required && !weighted)break` / rndNbt 管线）。8x6 藏宝楼=`ruinscraper/skyscraper_8x6_01`（CityGrid 内低权重地标）。

**P3 CoT 专属方块忠实重注册（2026-09-13·用户"CoT 方块可在本模组重注册，脚本+resources 在 RLCD1.1.2.b"）**：
- **背景**：上轮把 access_duct 的 `contentcreator:concrete_*` 降级成 polished_deepslate（丢失原味）。用户指路——CoT 方块定义在 `RLCD1.1.2.b/scripts/Dregora_ContentTweaker.zs`+`Dregora_ContentCreator.zs`(CraftTweaker VanillaFactory.createBlock)、贴图模型在 `resources/contenttweaker`+`resources/contentcreator`。
- **Underneath 用 7 个 CoT 方块**（全量扫描）：`contenttweaker:reinforced_concrete`(**3162 处**·rock/石声/硬30抗6000/镐3)、`contentcreator:concrete_slab/stairs_reinforced`(296/61·台阶楼梯)、`contenttweaker:iron_plate_rust_reinforced`(30·金属声)、`resource_crate`(24·金属硬10)、`bone_pile`(3)、`coolant_fluid`(1·稀有)。`iron_plate_reinforced`(非rust)→已映 quark:iron_plate 不重注册。
- **实装**：`ModBlocks` 加 6 方块(Block×4整块/SlabBlock/StairBlock，属性照 .zs)+`ModItems` 6 BlockItem 入 BUILDING_BLOCKS 创造栏；贴图从 resources 拷 6 张→`assets/underneath/textures/block/`；脚本生成 blockstates/models(整块 cube_all、slab bottom/top/double、stairs 40 变体)/item + lang×2；`block_map/modded.json` 7 条 CoT→`underneath:*`(coolant_fluid→water、resource_crate:0 统一 underneath 原 barrel)、删上轮 polished_deepslate 错映射。**教训：CoT/CraftTweaker 脚本方块可照 VanillaFactory 定义(材质/硬度/声/贴图名)在本模组原生重注册，比降级 vanilla 更忠实。**
- **验收**：access_duct 管道现为**强化混凝土/锈铁板真质感**(非粉非深板岩)；创造栏建筑页有 6 新方块。

**P3·OTG 城市体系 1:1 忠实移植（2026-09-14·用户"城市群根本没忠实移植"后全面重做·待编译）——本轮全部认知与实现**：
- **算法源（重看必读）**：`CustomStructurePlotter.plotStructures`(布点)+`BO4CustomStructure`(展开/回滚)+`BranchDataItem`(惰性掷骰)+`BO4BranchFunction/BO4WeightedBranchFunction`(掷骰公式)+`BO3.trySpawnAt/process`(散布检查)+`TreeGen.spawnInChunk`(Tree 资源)。全部逐段读过并移植。
- **原版布点真相（此前 GRID 网格全错）**：per-chunk 贪心——每个未处理 chunk 依次试起结构（rarity100/freq0=尽量密铺）、**4-pass 四向空地扫描**量连续可用区（边界=已占 chunk/无城群系）、fit **minimumSize**（required-only 展开包围盒，WBR 忽略；citygrid 实测 **16×16**）、**包围盒居中放进空地且当前 chunk 必在盒内**、无 FixedRotation 则 **4 向随机旋转**、多候选按 **timesSpawned 升序+尺寸降序**轮流。无论成败 chunk 标 plotted（挡扫描不挡分支）。城间距=**涌现**（16×16 空地需求+展开占地 30-40 把周边碎片化）。
- **展开真相**：结构 RNG=`randomForCoords(startX+8, 0, startZ+7, seed)`（**y 此刻恒 0**，start.y 在 checks 后才写）；掷骰发生在 **children 惰性生成时**（普通行逐候选 `nextDouble()*totalChance<=chance` 首中；WBR 轮盘 `chance>=r?中:r-=chance,clamp0`，**totalChance 空隙=不出、required 也无兜底**=spawner_city 2% 空地）；cycle 脉冲（required 阶段→optional 阶段；optional spawn 后立即递归其 required 子链）；**required 失败→rollBackBranch 级联**（城市边缘被占用/无城群系时自然裁剪；滚到根=整城取消→plotter 换位重试）；深度：required 不消耗、**候选 depth>0 重置 cur=0/max=depth**（L1 毛细）、optional 进入 `(max>0&&cur<=max)||required`；**BranchFrequency**（8c 大道端片=15 同名最小距）；**CanOverride optional 延后阶段**（城市件全 CanOverride→结构内不碰撞，但跨结构 isChunkOccupied+biome 所有分支都查）。**required 普通 Branch 解析期只留首候选强 100=OTG 官方行为**（readArgs 实证，数据 chance 全 100）。
- **数据实锤**：citygrid 头=`randomY min=max=50`（**城基 Y50**，非 64）+`Frequency:0`+CanOverride，根只挂 required `L4-M-Cross(y-4)`；街道=required 单向链（**无双向环**，旧"去重防环"是误判）；`l3-m-cross.bo4`=**空对象 0 方块 0 分支=L4 直路 WBR 里 50% 权重的"道路终结符"**（L1-L3 街网从 L4 无入口=原版设计，L3:4 件=4 个空根正常）；`skyscraper_3x3_06` 根**原 jar 就缺**（作者笔误 `_061C0R0`）=掷中留空楼位，忠实保留 missing；spawner_city=`highestSolidBlock 65-75/FixedRotation NORTH`→**只在穹顶破洞列出独立残楼**（穹顶完整列 highest≈250 越界即弃）；`access_duct`=**CustomStructure！highestSolidBlock 241-255=长在穹顶顶面**（13 群系，非我旧散布的主腔地板）；GodlySpike-50=`randomY 46..55→OTG 规范化 max<min 取 min=55`（Underspikes Edge+Pillar，Freq1）、Ceiling=Y190（Edge only）——两者=纯分支容器（128/88 条 optional 短名 WBR depth10 朝向簇）；**biome gate**：城市起点=12 核心群系（rarity100）、`Remnants Outter Edge` rarity0=只许分支伸入、**远古遗迹 8 群系无城**（分支伸入即回滚）。
- **Java 实装**：新 `bo/OTGStructureExpander`（BO4CustomStructure 完整状态机：cycle/回滚/深度/BranchFrequency/CanOverride 延后/AABB 碰撞/Env 回调（occupied+biome）/minimumSize 模式/randomForCoords；未移植=branchGroup/mustBe*/水面/SmoothingArea——**数据 0 使用已全量验证**）；`worldgen/CityPlotter` 重写（plotStructures 忠实：光锥 REGION=16+MARGIN=64 固定序模拟 populate（贪心密铺性质保持、免存盘确定性）、4-pass 扫描、fit/旋转/落点公式逐行照搬、5 结构 DEFS 表（citygrid/spawner_city/access_duct/godlyspike-50/ceiling 各带起点/分支群系名单）、spawner_city+access_duct 的 highestSolid 用 OTGTerrainMath 密度求列地表（腔内目标先穹顶带粗采样 fail-fast））；`CityPlotFeature` 传 BiomeGate（BiomeSource+Climate.Sampler 按 OTG x+8/z+7 列采样）；`/underneath city` 改调新引擎（FREE 环境理想城快测）；**删 CityExpander/CityStructure/CityBoPiece/worldgen/structure JSON/STRUCTURE_TYPE 注册**；`has_city` tag 收窄 21→13（12 起点+outer_edge=放置面）。
- **城市废墟散布系（同轮补齐·原版 .bc Tree/CustomObject 行忠实移植）**：`BOScatterFeature` 加 `mode:"tree"`（TreeGen 语义：attempts 次×逐候选掷 chance、**每候选独立坐标、生成失败继续下一候选**——chance 全 100 的多候选因此都活）与 `mode:"custom_object"`（BO3.process：每对象独立按**自身头** Frequency/Rarity）；两模式 Y/旋转从对象头取，放置走新 `BOPlacer.tryPlaceOtg`=OTG trySpawnAt 语义（**BlockCheck/BlockCheckNot 前提检查（坐标随旋转变换）+SourceBlocks 逐块统计（outside 超 MaxPercentageOutsideSourceBlock 预算→整体放弃；dontPlace→outside 位置跳过）**）。新 `bo/BlockMatcher`（MaterialSet 移植：Solid=blocksMotion/AIR/无 meta token 经 legacy 表 :0..15 家族展开（CONCRETE=16 色）/1.12 数字 id 微表 4 条/现代 id 注册表直查兜底）；BOObject+BOParser 加头字段（CanOverride/BranchFrequency/Frequency/Rarity/SourceBlocks/MaxPct/Outside/BC 行）。**特征清单（.bc 逐文件实证）**：destructor_roads(Tree10,02+03=路面剥蚀 SourceBlocks 灰混凝土)/destructor_main(Tree25,Underneath 主群系 dense=30；01=全城市材料 MaxOutside0=整体嵌入楼群才放——**旧"8次/chunk 轰碎主腔"正是缺此检查**)/sewergap(Tree50@15%)/chestfiller top·higher·upper(Tree85)+lower(Tree25@40%)(SourceBlocks:AIR MaxOut0+BC 脚下城市楼板；12+outer_edge=13 群系)/lattice(CustomObject：18 件各 Freq200 R100 Y100-250+BC 脚下(Stand)/头顶(Hang)混凝土——只贴楼生长；Underneath+Corrupted)/**ashen_rock floor·ceiling·pillar 重加**（CustomObject Freq20+`SourceBlocks:Solid MaxOutside40 dontPlace`=用户要的依附检查；ceiling+floor=6 核心+ancient_remnants×6、pillar=7 个 Edge/Pillar 系）/beckon 挂载修正(corrupted+corrupted_edge)。**cityspawners/citycobwebs/cityvine/bloodspawner/cityroadend/rlcraftspawner/airlayer=全 .bc 零引用孤儿，原版也不生成，忠实=不做**。gen_biomes.py 全部数据化重跑 ✓（散布 22 对、引用/顺序校验过）。
- **离线验证（沿用方法论：改 worldgen 必先 Python 同构模拟）**：`scratchpad/sim_otg_city.py`（JRandom+引擎逐行同构）+`sim_plotter.py`（plotter 全流程）：城 373-388 件/22×34 chunk 有机形态/建筑 100+ 切片种类/种子敏感/minimumSize 16×16/遮挡→整城回滚 ✓/13 城互不重叠 overlap=0/最小间距 18.4chunk/4 向旋转/spawner 空地率 1.5%（预期 2.1%）。
- **既知偏差（记录）**：①populate 探索序→固定区域序（光锥截断，贪心密铺性质与观感不变，免存盘）；②spawner_city/access_duct 的 highestSolid 用纯密度（无 carver）——carver 挖的穹顶洞看不见→独立残楼比原版略少（调参口=resolveStartY）；③plot 决策 RNG 用 per-chunk 派生（OTG 用非确定 populate rand）。
- **性能预期**：region 首算 ~1-3s（~30 座城展开+若干失败回滚+spawner 列采样），缓存后不卡；用户已拍板可接受。**验收见顶部快照**。**首编风险点**：BOScatterConfig 9 字段 codec、BlockMatcher 的 blocksMotion、CityPlotFeature 的 randomState()/getNoiseBiome、OTGStructureExpander 泛型/ChunkPos.getX(long)——报错发全文即修。

**P3·首测三修（2026-09-14·用户图一"主腔零城市"+图二"穹顶下规则建筑无破洞"·已修待编译）**：
- **诊断法（沿用同构模拟）**：Python 复演 5 结构完整 plotter → **精确复现病征**：citygrid 仅 9 座+expandfail 2072、access_duct 2801 段铺满。恶性循环=access_duct（1×1 见缝插针+成功率 97%）密铺 → 2D chunk 竖列独占把空间碎成 2×3 颗粒 → citygrid 的 16×16 fit/30×40 展开全灭 → 更多空位给管道。**图二的"规则建筑点阵"=数千个 access_duct 维修舱**（根锚穹顶顶 251、舱体经 `WBR(0,-110,0,...)` 悬在主腔上空 Y~141，2×3 chunk 小舱含床/灯/盖革计数器 nbt——在 cave 模式小地图上呈规则蜂窝阵）。
- **修①组间距（根因）**：access_duct 头有 **`BO3Group: access_duct:10`**——OTG `isBO4AllowedToSpawnAtByFrequency` 第二段 bo4Groups：同组结构**中心**距 ≤max(组频率,已记录半径) 则拒绝。我 DEFS 没实现 → 已补：Def 加 group/groupFreq、plotter 加 spawnedByGroup 登记（中心=OTG bo4CenterSpawnCoord 公式）+检查。godlyspike 的 `NewCrystals:0`=频率 0 无效果（OTG >0 才登记）；citygrid/spawner 无组。**修后模拟：19 城/49 舱/expandfail 212（正常损耗）/零重叠/间距 18.2**。
- **修② rot 丢失**：tryPlotAt 算好的随机 rotation 没传给 expand（写死 0）→ 城恒 NORTH。已改传 rotation（模拟实证 rot 1/2/3 都出）。
- **修③穹顶破洞**：原版 WorldConfig.ini `CaveRarity:7/CaveFrequency:40/CaveMinAltitude:8/**CaveMaxAltitude:255**/RavineRarity:0`——**1.12 洞穴全高度雕刻切穿穹顶=破顶洞的真正来源**；我挂的 vanilla carver（cave y≤180/extra y≤47/canyon y≤67）全够不到穹顶 Y196-252 → 新 `worldgen/configured_carver/cave.json`（vanilla cave 参数、y [8,255] 绝对、lava_level absolute 10、probability 0.14）；gen_biomes carvers 改 `["underneath:cave"]`（canyon/extra 删）。已重跑生成器。
- **修③b carver 崩溃修复（2026-09-14·crash 报告实锤）**：首版 cave.json 漏 **`replaceable` 字段（1.20.1 CarverConfiguration 必填）**→ 注册表加载期 `No key replaceable` → 点单人游戏即崩。已补 `"replaceable": "#underneath:carver_replaceable"` + 新 `tags/blocks/carver_replaceable.json`（vanilla overworld tag + smooth_basalt + epca/defiled 系 required:false——不加则洞穴雕到玄武岩带/虫染带/污秽石留壳）。**该 crash 顺带证明：全部 Java 编译过、biome/feature/scatter 注册表校验全过（错误清单里只有 carver 一项）**。
- **修③c 第二崩（2026-09-14·`Not a JSON object: null`×4）——⚠️通用教训：FloatProvider 与 HeightProvider 的 uniform JSON 格式不同**：HeightProvider（y 字段）=平铺 `{"type":"minecraft:uniform","min_inclusive":{锚},"max_inclusive":{锚}}`；**FloatProvider（yScale/半径乘数/floor_level）=包一层 `value` 且上界叫 `max_exclusive`**：`{"type":"minecraft:uniform","value":{"min_inclusive":0.1,"max_exclusive":0.9}}`。我 4 个 float 字段按 HeightProvider 平铺写 → 4 条 "Not a JSON object: null"（codec 取 "value" 键得 null）。**方法论：写任何 worldgen JSON 前先 `unzip -p 版本jar data/minecraft/worldgen/<同类>/xxx.json` 抄官方原文对拍，别凭记忆写 codec 格式**。已按官方格式修正+结构机器校验过。
- **既知残余**：spawner_city 的穹顶破洞判定用密度公式，carver 洞看不见 → 独立残楼仍偏少（原版靠真 heightmap）；如需提升 → feature 阶段真方块二次判定（架构成本大，先观察）。碎楼判定与破洞率待用户实测反馈再调 probability（0.1~0.2）。
- **验收（重编新世界）**：①主腔应现大量完整城市（旋转多样、街道连通、城间血湖缝）；②管道舱变稀疏散点（~10 chunk 间距）；③穹顶出现破洞（洞下偶见独立残楼）；④`/underneath city citygrid 6` 快测回归。

**P3·二测三修（2026-09-14·城市已生成✓·用户"建筑过度残破/大使馆岩浆地下室重叠/主腔无血池"）**：
- **修①建筑过度残破（根因=缺 OTG populate 域检查）**：OTG `BO3.trySpawnAt` 对每个方块查 `OTG.IsInAreaBeingPopulated`——**对象必须整体落在触发 chunk..+1 的 2×2 chunk（32×32 方块）区内，出界=整个对象放弃**。30×30 的 destructor 起点=chunkMiddle(+8)+rand16 → 几何通过率仅 **3.5%**（x/z 各 3/16 窗口）；我们没实现=100% 通过 → destructor 命中率高 ~28 倍 → 城市炸成筛子/楼缺面/8x6 藏宝楼岩浆宝库被炸穿漏浆流进邻楼（="岩浆地下室与建筑重叠"观感）。**修**：`BOPlacer.tryPlaceOtg` 加 popMinX/popMinZ 参数（AABB ⊂ [popMin,popMin+31]² 才放，用 worldBounds 旋转 AABB O(1) 判定，localBounds 已补随机块坐标）；`tryOtgSpawn` 起点同步 OTG 语义 **+8+rand16**（原 +0..15 偏半个 chunk）。ashen 三件也是 30×30 → 同被压稀疏（正好消化 P2"灰岩盖满穹顶"旧病）；sewergap 15×15/lattice 1×1/chestfiller 1×1 影响小。**尺寸>32 的对象在原版同样 100% 失败=忠实**。
- **修②主腔血池（根因=sea_level 太低）**：原版 `WaterLevelMax: 63`（维度水=血）——主腔地板（顶面 Y64+，城市路面亦 ~64+）低洼处 Y63 下积血=原版城市周围的大片血域。sea_level 33→**63**（default_fluid=blood 已有）；顺带把 P2 一直搁置的"水环 Y50-63"自动还原。偏差：Y16-50（原版 ReplacedBlocks 抽干段）我们会灌血——藏在中层板下不显眼，P4 再议。城市街道不会被淹（路面在 63 上；城内 AIR 为 feature 覆盖地形、无 fluid picker）。
- **修③beckon 从未生成（顺手实锤）**：`underneath_beckon_spawn.bo3`=**纯 Entity 行**（`Entity(0,1,0,srparasites:beckon_sii,1)`+BC 脚下实心）——BOParser 一直跳过 Entity 行 → 0 方块对象被空判定拒绝（P2 至今从未出过）。**修**：BOObject 加 EntityLine+BOParser 解析 Entity/E 行+`BOPlacer.spawnEntities`（entity_map 映射→EntityType.byString→finalizeSpawn(STRUCTURE)+setPersistenceRequired+addFreshEntityWithPassengers，ServerLevelAccessor 守卫）；scatter 空判定放行纯实体对象。城市 bo4 零 Entity 行=此实现即全覆盖。
- **旋转翻译复核 ✓**：OTG `Rotation` 枚举实锤=NORTH(0),WEST(1),SOUTH(2),EAST(3)——与 BORotation/plotter spawnC 公式翻译一致，"缺面"非旋转错位（是 destructor 炸的）。
- **验收（重编新世界）**：①建筑破损度大降（ruinscraper 天然废墟感为主、destructor 偶发蚀洞）、8x6 藏宝楼完整（黑曜石宝库不再漏浆）；②主腔无建筑处=血池/血湖面（Y63）、城市贴血而建；③Corrupted/Corrupted Edge 地下 Y10-25 偶见 beckon 召唤者（epca:stage_ii_beckon）。破损度仍不满意的调参口：destructor 两特征 attempts（10/25/30）。

**P3·三测六修（2026-09-14·用户"底腔血淹/钟乳石碎/釉陶乱/门窗栏不连/箱穷"全拍板·已落地待编译）**：
- **修①ReplacedBlocks 忠实移植**：原版 .bc `(STATIONARY_WATER,air,10,50),(biomesoplenty:blood,air,10,50),(STATIONARY_LAVA,quark:basalt,0,255)`——新 `worldgen/ReplacedBlocksFeature`（注册 `underneath:replaced_blocks`，raw_generation 步=carver 后/一切结构前→建筑自带岩浆宝库不受影响）：Y10-50 血→air（底腔变干陆地+传送门遗迹 floor 扫描自动回归、只留 Y50-63 水环）、岩浆→smooth_basalt（只扫 Y0-50——岩浆唯一来源=carver lava_level Y10）。gen_biomes 挂全 21 群系 feats[0]。
- **修②邻接形状修正（钟乳石/门/玻璃板/栏杆/栅栏/墙/楼梯转角/双箱统一根因）**：1.20 连接态靠放置时邻接更新、1.12 数据无此属性、worldgen flag 2 不触发→全默认态。`BOPlacer` 四放置路径（place/placeOwnBlocks/placeOwnBlocksClipped/tryPlaceOtg）统一：收集连接类方块（instanceof 白名单：CrossCollisionBlock/Wall/Door/Stair/FenceGate/PointedDripstone/Chest/RedStoneWire/TripWire/Vine），放完后 `Block.updateFromNeighbourShapes`（StructureTemplate 同款，flag 2|16）+对 6 邻已存连接块双向刷新（跨 chunk 边界两侧都伸臂）。
- **修③映射补两族（"莫名其妙釉陶"真相=不缺方块）**：品红占位实为映射表漏了 Dregora 混写的 **1.12 现代 id**：`minecraft:wooden_slab:5`(深色橡木台阶×4280=楼板主力)+17 色 `minecraft:*_glazed_terracotta:朝向`(×3400,silver→light_gray)。gen_legacy_map 补 wooden_slab/double_wooden_slab/stone_slab(2)/釉陶族(byHorizontalIndex 0=S,1=W,2=N,3=E)→1523 条；**BlockMapper.lookup 加"剥 minecraft: 前缀重查"回退**（通杀所有现代名写法）。复扫城市 Block 行 miss=0。
- **修④chestfiller 楼板箱必空（rndNbt 缺口清账）**：`BOFlattener.Flattened` 加 `String[][] rndNbt`（扁平化期解析全路径）；place/tryPlaceOtg 掷中候选应用（applyFlatRndNbt）——placebo/scatter 容器 loot 的 P4 欠账一并清。城市楼板将成对出现【寄生虫笼(y+1)+有货箱(y+2)】。
- **修⑤裸刷怪笼→寄生虫**：`QUARK:MONSTER_BOX→minecraft:spawner` 映射产物无 SpawnData=猪笼——四放置路径 rnd 掷中无 nbt 且是 spawner 时 `fixBareSpawner`（SpawnerBlockEntity.setEntityId(epca:ripper)，缺 EPCA 静默跳过）。
- **修⑥武器海回归（"8x6 箱有货但穷"根因）**：P1 的 `rlcraft/loot_bundles/**` 深链截断（→common/epic_loot 降级替代）撤销——`port_loot_tables.py` 删截断分支+REF_FIXED 提前（golden_loot 原包断链→就近 golden_tools）+STALE_FIX 补 `quiver_arrow→small_arrow_quiver`。重跑 **270→404 表、零缺源、零未映射物品**——8x6 宝库/城市箱开出 RLCraft 全系装备的 1.70 对应物。楼体"箱子"实为发射器/投掷器（药水/马桶杂物=原版设计寒酸，非 bug）；8x6 地下压力板房（铁 448/金 113/石 34）=原版机关房设计+ruinscraper/skyscraper 两变种，无缺失。
- **验收（重编新世界）**：①底部空腔=干陆地+血团+传送门废墟、Y50-63 水环、无裸岩浆；②钟乳石连贯、门整扇开关、玻璃板/栏杆/栅栏连臂、楼梯有转角；③城楼内无品红占位（深色橡木楼板+彩色釉陶地砖回归）；④楼板笼箱组合有货、8x6 宝库开出武器海；⑤`/underneath placebo loot_misc` 回归。

**P3·四测收尾（2026-09-14·用户测六修版仍"箱穷/笼箱少/传送门只有几个"）**：
- **箱穷的最后一环实锤**：六修的捆包重建**新暴露 49 个非法物品**（charm 唱片×26/rlmixins 钢甲/spartandefiled/spartanshields/金系 better survival/VC 大剑等）——**未知物品 id=解析期废整张表**（P1 铁律），恰恰废的是 cursed_weapons/gilded_tools/rlcraft_music 等**富表** → city_switch 掷中富分支也开不出 → 六修版依旧穷。修=mod_rules 加 2 规则（charm/BOP 唱片→vanilla 12 碟稳定轮换；IAF `troll_weapon.X`→1.20 真身 `troll_weapon_X`）+MOD_ITEMS 补 22 条（pigstep→原版 pigstep 碟/NTP 燧石镐→simple_difficulty/钢甲→铁甲/umbrium→diamond 斯巴达/盾→bettersurvival 大小盾/金锤斧→spartan golden 系/VC 恶魔冰霜大剑→spartanfire 龙骨大剑）。**重跑+三维校验（SNBT/物品/属性）全零：404 表 194 捆包完全合法=武器海完全体**。快验指令：`/loot give @s loot underneath:rlcraft/loot_bundles/diamond_weapons`。
- **对拍结论存档**：toilet/fish_undead/city_switch 等移植表与原版**逐池逐条目权重全同**——可可豆箱=马桶垃圾表（1.12 brown_dye=可可豆）=原版忠实；楼体"箱子"=发射器（city_potions×263/toilet×144 每 3x3 楼）=原版寒酸设计；直装 Items 全项目仅 46 个（paper/book/torch 系）非穷因。
- **传送门"只有几个"实锤=floor 扫描下界 bug**：portal_ruins_low 的 min_y=18 而腔底地板顶面≈Y15-17（`for y>minY` 扫不到）→ 只有噪声凸起处命中。修 min_y 18→12（gen_biomes 已重跑）。
- **笼箱数量结论**：楼体自带 0 笼=原版；chestfiller 四档 Y 带与四档楼高精确对应（3x3 顶 249↔top 200-255/2x2 顶 191↔higher/8x6 顶 109↔upper），密度参数=原版 Tree 行忠实——箱有货后存在感回归，仍嫌少的调参口=chestfiller 四特征 attempts（85/85/85/25）。
- **四测补修·斯巴达系真身归位（2026-09-14·用户指出"包里其实有污秽斯巴达，前置 id 不一样"实锤）**：①**污秽斯巴达在包内**=`defiledspartan-1.0.0.jar`（1.12 modid `spartandefiled`→1.20 **`defiledspartan`**，词序反转 `saber_umbrium`→`umbrium_saber`，umbrium+scarlite 全 24 类型在，lang 实锤）——3 条钻石斯巴达降级已改回真身；②**SpartanShields 1.20.1 官方版在包内**（modid 未变、命名反转 `shield_tower_wood`→`wooden_tower_shield`，银塔盾等全注册）——5 条 bettersurvival 盾降级改回真身（连 silver_tower_shield 都精确归位）；③唱片池扩容：charm/BOP 27 张自定义碟的替代池从 vanilla 12 → **34 碟**（+quark9/betterend6/betternether3/biomeswevegone2/supplementaries1/takesapillage1；排除 cataclysm 9 张 boss 专属碟防贬值）；④rlmixins 钢甲确认包内无真身（spartanshields:steel_* 只是盾）维持铁甲。重跑 404 表+三维校验三零。**教训：判"缺席"前先按 jar 文件名扫 mods 目录（modid 常改名：spartandefiled→defiledspartan、spartanfire→spartanfire_rlc），pack_items 名单 grep 旧 ns 落空≠mod 不在。**

**P3·五测拼缝大修（2026-09-14·用户对比图"1.12 完美十字+道路两旁必有密楼 vs 我们太乱"·已落地待编译）**：
- **数据实锤（citygrid 分支链逐层读通，判病征来源必读）**：城=citygrid→required `L4-M-Cross`（4×4 切片 required 链互拼）→四向 START→2c/3c/8c 直路段链；**楼位=required WBR 且 Σchance=totalChance（3x3 楼 51/51、2x2 楼 50/50）→原版必出楼**；**道路延续=required WBR total20=Σ（2CC0R1 5+5 直行/L3-M-Cross 10 转十字）→原版无断头路**；8x6 藏宝楼挂在 8cc0r7 端片（唯一 BranchFrequency:15）。→ 城稀碎只能是引擎把选中分支拒掉，不是数据。
- **根因（同构模拟实锤·`scratchpad/sim_seam*.py`）**：**光锥 region 拼接对贪心密铺强耦合系统不收敛**——先起的城挡后起的城、影响链式传播任意远；REGION=16 独立模拟拼接实测**窗口 61% chunk 城市归属不一致**：城被撕成 16-chunk 拼布、道路在 region 边界错位断头=用户图二全部病征。Python 模拟从没暴露它，因为模拟一直是单次全域跑（无拼接）——**教训：近似方案的"近似误差"本身也要进模拟对拍**。
- **中间方案已否决（勿再走）**：超区独立贪心+跨界整城淘汰（保守局部规则）——SUPER=64 城死 53%、SUPER=128 仍死 35%、覆盖率 35.8% vs 真值 59.1%、缝带整条空白（压死无人补位）。
- **终案＝SavedData 增量贪心（OTG 存盘 structurecache 的机制同构，"免存盘确定性"原则废除）**：新 `worldgen/CityPlotSavedData`（plotted/occupied/**generated**(feature 已执行=丢件精确判定)/pending(chunk→零件待领)/timesSpawned/startsByName/spawnedByGroup 全持久化；**并发契约=一切读写持 data 实例锁**，save 自锁同一把；**DimensionDataStorage 非线程安全→主类 `LevelEvent.Load` 主线程预热**）。`CityPlotter` 删光锥全套（REGION/MARGIN/plotRegion/REGION_CACHE），`chunkPieces(ServerLevel,cx,cz,gate)` 改逐 chunk 增量：feature 触发≈OTG populate 探索序，未 plotted 即试起城（4-pass 扫描/fit/旋转/展开全保留），件按世界 AABB 分桶入 pending、所在 chunk 生成时领取。**确定性取舍**：同种子重建世界布局不同——OTG 原版本就如此（populate rand 非确定），忠实。
- **既知偏差**：城分支伸进"已生成 chunk"的件份额只能丢弃（OTG 1.12 直写已生成区块、1.20 feature 只能写本 chunk）——探索前沿总向未生成方向扩，低频，debug 日志计数。
- **模拟器资产已同步**（曾漂移：无组间距/rot 写死 0）：`dev/sim_plotter5.py` 回灌 BO3Group 组间距+rot 传参+`plot_rect` 矩形域，复跑 81×81=19 城/49 舱/expandfail 212 与首测修①记录一致 ✓。**改 Java 引擎须同步改模拟器**铁律再犯一次，勿再。
- **五测验收（重编+新建世界）**：①城市大而连贯（单城数百 chunk、笔直大道贯穿、**完美十字路口**、道路两旁密集楼=required WBR 必出）；②城间blood湖缝=涌现间距（~18 chunk）；③**无 16-chunk 错位拼缝/断头路**；④退出重进+跨 region 探索城市延续不错位（SavedData 生效）；⑤`/underneath city citygrid 6` 快测回归；⑥远探索存档体积增长可接受（LongArray 集合）。观感仍嫌稀→调参口=expandfail 碎片带（OTG 原版行为，慎动）。

**P2④⑤⑥ 矿物/血泉/植被/分布 已实装（2026-09-13·待编译验证）——P2 全系列代码收官**：
- **生成器**：`dev/gen_biomes.py` 升级为 **P2 总生成器**（群系+矿物+血泉+散布+lang 一体，改任何认知改它重跑）→ `worldgen/{configured,placed}_feature/` **34 对**（19 矿+2 泉+13 散布）+ 12 biome 的 features 数组（槽 6 矿/8 泉/9 植被）。
- **P2④ 矿物**：每群系 Ore 行自动解析→`ORE_BLOCK/SOURCE_BLOCK` 映射（EPCA 虫染重矿×4/心水晶×2/原版深板岩矿×6/defiled 四矿/corundum 水晶）；count=freq×rarity 折算；**全局字典序+真环检测=feature-order-cycle 安全**。`quark:crystal`→**corundum 统一升级**（1.12 色序 meta0=红/1=橙/7=白；modded.json 原 amethyst 占位一并升级——corundum 模块已验证开启）。Underspikes 第 13 矿=`parasiterubbledense:1` 巨脉(size33×20,Y60-70)源写 STONE 疑为原版死行→源取意图映射 deepslate 让虫染巨脉真出现。**血泉**：`spring_blood_8_60/140_255`(count20, underneath:blood, valid=deepslate/smooth_basalt/stone_defiled)。
- **P2⑤ 植被（Java+数据）**：`worldgen/BOScatterFeature+BOScatterConfig`（注册名 `underneath:bo_scatter`，`init/ModFeatures` 挂 mod 总线）——OTG Tree/CustomObject 语义：attempts×(顺序掷选第一命中即放)；**anchor 两模式**：floor=区间随机起点下扫"空气踩实心"+on_blocks 着块白名单(运行时按名解析缺 mod 跳过)，random=忠实 OTG randomY 无检查（**浮空灰岩尖刺/埋藏废墟正是靠它**）。`bo/BOPlacer` 从 placebo 指令抽出共用（指令已重构调它）；**worldgen 阶段方块实体 skip**（TileEntityConverter 仍 ServerLevel 专用，P3 城市放置时泛化）；BORegistry ConcurrentHashMap 并发安全已核。**13 个散布特征**：ashen_rocks floor(Y0-10)/ceiling(Y100-200 随机=浮空尖刺)/pillar(Y8-200)、枯树 big/tall×underforest(草上)/defiled(污秽草上) 各20种10%、石笋+basalt_speleothem、capilarry_nets/invested_veins(虫染石上)、beckon_spawn(Corrupted 限定 Y10-25 80%)、portal_ruins low(Y10-16 40%)/high(Y60-70 20%)。**City 系(Destructor/ChestFiller/Sewer/Lattice/GodlySpike)全部推 P3**（依赖 L2 城市）。
- **P2⑥ 分布收尾**：7 个 Edge/Pillar/Hole 变体以**外圈薄壳**入 multi_noise（共 12 群系；map.png 精确分区仍留 P4 可选）；**Defiled 全深度污秽石**=surface_rule 末尾 biome 兜底规则（该群系全部 default_block→stone_defiled，且顺序在噪声条带后=条带保留）。
- **校验已过**：全 JSON 语法/placed→configured→biome 三级引用闭合/真环检测(step6/8/9 无环)/维度 12 群系闭合。**解析期硬引用 mod 方块 19 个**（epca×7/defiledlands×7/quark corundum×3/simple_difficulty×2，全部 jar 实锤在 1.70）——缺任一 mod 的环境维度拒载（仅服务 1.70 原则）。
- **P2 整体验收（重编后新世界）**：①地形/群系/表层同前述各步；②矿：主群系深板岩里可见虫染重矿(Y<75)+深层原版矿+basalt 里橙 corundum；Defiled 有污秽石+四矿；Underspikes Y60-70 有虫染密岩巨脉；③血泉：低带 Y8-60/高带 140-255 岩壁渗血；④植被：Underforest 主腔草地上 40 种枯树、Underspikes 石笋簇+虫染网/脉、各处浮空灰岩(Y100-200)与灰岩柱(变体群系)、Corrupted 埋藏 beckon、低带埋藏传送门废墟+主板面破损传送门；⑤`/underneath placebo` 回归不坏（BOPlacer 重构验证）。已知延后：城市系植被(P3)、水环 Y50-63(P4 可选)、Sapling 树苗生长表(P4)。

**待用户拍板**：仅剩 ①P3 分支引擎时机（先交付 P0~P2 再评估？）。（目标包/特种矿/血肉三项已定，见上方「三项拍板已定」。）

## 七、参考索引（仅需要时查阅）

- **OTG 1.12 源码**：`E:\OpenTerrainGenerator-1.12-OTG-`
  - BO 解析：`common/.../customobjects/bo3/BO3Loader.java`、`bo4/BO4Loader.java`、`bo3function/BO3BlockFunction.java`(58)、`bo4function/BO4BlockFunction.java`(104)、`bofunctions/BranchFunction.java`
  - 分支引擎（L2）：`customobjects/structures/bo4/{CustomStructurePlotter(1193),BO4CustomStructure(2271),BranchDataItem(133),BO4CustomStructureCoordinate}.java`、`bo4function/BO4BranchFunction.java`(210)
  - 方块系统（映射表锚点）：`util/minecraft/defaults/DefaultMaterial.java`、`common/LocalMaterialData.java`、`platforms/forge/.../materials/ForgeMaterialData.java`
- **原预设 jar / 打包工具**：`...\RLCD1.1.2.b\mods\Underneath-2.0.jar`；`E:\WorldPacker-1.16.4`
- **EPCA（归寄万物，替代 srparasites）**：命名空间 `epca:`，jar 见上
- **提取的预设数据**：`<scratchpad>/un_extract/assets/worldpacker/Underneath/`（WorldConfig.ini / WorldBiomes/*.bc / WorldObjects/*.bo3,*.bo4）
- **框架蓝本**：`E:\RLStructure`（本工程框架照搬来源）
- **完整分期文档（首版）**：`<scratchpad>/Underneath_1.20.1_移植分期实施文档.md`（已发用户）
- **离线验证工具（dev/，P3 城市调试三板斧）**：`sim_otg_city.py`（JRandom+OTGStructureExpander 逐行同构，改展开逻辑先跑它）、`sim_plotter.py`/`sim_plotter5.py`（CityPlotter 全流程复演：布点/组间距/碰撞/分布图）、`verify_bundles.py`（loot 表三维校验 SNBT/物品/属性）。改 Java 引擎须同步改模拟器保持同构。
- **原版 .bc/WorldConfig 快速重解**：`unzip -o -j -q RLCD1.1.2.b/mods/Underneath-2.0.jar "assets/worldpacker/Underneath/WorldBiomes/*.bc" "assets/worldpacker/Underneath/WorldConfig.ini" -d <目标>`（scratchpad 会话丢失后随取随解）。
